policy: "choreography"
choreography_conf {
  choreography_processor_num: 1
  pool_processor_num: 1
  # Perception 是唯一指定的定向任务；其余四个 Component 必须回退到
  # cyber_ref Choreography 的 Classic 公共池，不复制 DAG 或私有业务队列。
  tasks { name: "perception" processor: 0 prio: 5 }
}

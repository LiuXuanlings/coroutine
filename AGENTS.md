# MiniCyber Agent 工作约束

本文件是第二次重构后续会话的第一入口。执行前先读取本文件，再读取
`MINICYBER_ROADMAP.md`、`docs/refactor/00_进度记录.md`，并按照路线图规定
的顺序读取 `01_实施方案.md` 至 `05_实施归档.md`。当前任务和状态仅以
`00_进度记录.md` 为准。

## 十条红线

1. 除代码、CMake 官方指令和英文专有名词外，所有 Markdown 使用中文。
2. 保留职责必须对照 `cyber_ref` 的原生设计；可以裁剪范围，不得自创优化、
   伪装原生能力或改变保留职责语义。
3. 发生重大 Bug、泄漏、死锁、ABI、生命周期或选型变更时，同一任务同步更新
   `docs/DEBUG_MANUAL.md` 和/或 `docs/INTERVIEW_QA.md`。
4. 只在 `development` 创建 Commit 和推送，严禁直接提交或推送 `main`。
5. 一个 MC 任务只对应一个 Commit；不得跳过、合并任务或夹带下一任务修改。
6. 验证与风险相称：保留高风险单测和完整业务集成测试，不以堆叠无业务价值
   的测试数量替代任务卡主验收。
7. 开始前先把 00 台账中的当前任务标为“进行中”；完成后记录真实验证、Commit
   和 Push，再标为“已完成”。现有未提交修改默认属于用户，只暂存当前任务文件。
8. 生产代码任务必须同步补充意图注释：公开 API、协议编号、并发状态机、所有权、
   回滚和关闭顺序至少说明关键不变量及对应的 `cyber_ref` 职责；禁止逐行翻译代码。
9. 每个生产代码任务都要在同一 Commit 增量更新 `docs/INTERVIEW_QA.md`；重大排错
   同步更新 `docs/DEBUG_MANUAL.md`，不得把过程知识统一推迟到 MC-621/MC-622。
10. 每任务结束同步检查路线图、00、04、05 的当前指针和产物状态，并在 00 记录
    01/02/03 的适用性检查结果；Commit 证据使用完整 40 位 SHA 和 subject。

## 任务交付回报

开始编码前，Agent 必须先回报当前任务、上一步输入、原生职责映射和计划执行的验证
门禁。结束时必须交付代码路径、实际测试结果、文档锚点、完整 Commit SHA 和
`origin/development` 同步状态五项证据。只有构建通过但任务卡行为未验证，不得完成任务。

已在 `docs/refactor/02_架构取舍矩阵.md` 和
`docs/refactor/03_验收与性能口径.md` 定稿的决策不得重复询问。只有源码事实
与定稿决策直接冲突、外部依赖缺失，或任务必须越过白名单时，才可在 00 中记录
证据并标记阻塞。

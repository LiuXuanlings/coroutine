# MiniCyber 重构进度记录

## 接力规则

这是后续会话的唯一进度事实来源。新会话必须先读 `MINICYBER_ROADMAP.md`，再读本文件，并按路线图顺序阅读 `01_实施方案.md` 至 `05_实施归档.md`；最后阅读当前任务卡指定的 `cyber_ref` 和 MiniCyber 精确文件。MC-601 创建根级 `AGENTS.md` 后，后续会话必须最先读取该文件。

开始任务前将状态从“待开始”改为“进行中”，完成后填写真实 Commit、Push、验证命令和结果并改为“已完成”。无法继续时改为“阻塞”，记录原始证据与待确认问题。不得跳过、合并任务，不得重复询问 `02/03` 已定稿决策。

## 当前状态

- 当前阶段：第二次重构 / 业务模块改造阶段
- 当前任务：MC-619（完成 Choreography、混合扇出与文件触达审计）
- 当前状态：待开始
- 当前分支：`development`
- 最近更新：2026-08-15
- 当前基线：MC-001 至 MC-504 是第一次重构历史；第二次重构需求、架构和验收已在路线图与 `01~05` 定稿。
- 下一指令：执行 MC-619。接收 MC-618 的 Classic 主干 CTest、原生 SHM 首档块池和真实失败
  记录；只执行 Choreography、混合扇出与文件触达审计，不提前实施 MC-620 性能采集。

### MC-616 交付质量评估修正记录

- 日期：2026-08-15
- 性质：用户授权的不占 MC 编号质量评估提交；不改变 MC-617“待开始”，不提前实施 MC-618/619 的正式集成验收。
- 状态：已完成，待本评估 Commit 与 Push 证据收口。
- 待闭环：metrics 关闭仍保存延迟样本，重复与乱序统计混合，ControlAudit 缺少业务结果集合证据，正常路径 SHM 自然回收被无条件兜底清理遮蔽，业务 Runtime 与核心库边界不清，04 的信号机制描述失真。
- 原始失败证据：评估首次执行 `--messages 20 --frequency 100 --timeout-ms 10000` 时脚本退出码为 3，Sink 收到 `14/20`；同参数后续连续 3 次通过，当前观察窗口失败率为 1/4。默认 `1000/100 Hz` 复验通过。该现象作为未确认的启动/排空窗口保留，不得被后续通过记录覆盖。
- 验证要求：Debug 全量构建和 CTest；metrics 开关、重复/乱序/丢包定向测试；小负载与默认负载主干重复运行；ControlAudit/ControlSink 集合一致；成功路径在兜底删除前证明精确 SHM 名称已自然回收。
- 修正结论：ControlSink 以独立集合统计 Control/Audit、丢包、重复和乱序，metrics 关闭时不保存延迟样本；Source 等待 sequence 0 端到端穿过 Audit 后才发布测量序列，避免 `ChannelBuffer` 首次取最新跳过首条测量数据；DAG 队列深度为 1024，覆盖默认验收窗口中预热后的调度抖动。成功路径先验证 SHM 自然回收，失败路径才精确兜底清理。业务模板实例移入常驻 `minicyber_autodrive_runtime` 共享库，通用核心恢复业务无关。
- 真实验证：`cmake --build build/debug -j2` 退出码 0；`ctest --test-dir build/debug --output-on-failure` 为 46/46 通过；20 条/100 Hz 在修正过程中连续 5 轮完整；最终默认 1000 条/100 Hz 连续两轮均为 `received=1000 audit_received=1000 lost=0 duplicates=0 audit_duplicates=0 out_of_order=0 audit_difference=0`；`--no-metrics` 小负载退出 0 且不生成 metrics 文件；所有成功运行均通过四频道自然回收检查。
- 文档同步：Roadmap 只增加局部门禁路由；01 将指标开关、SHM 自然回收、Hybrid 扇出证据和独立序列统计分别下沉至 MC-617~620；03/04/05、DEBUG 与 INTERVIEW 已同步当前事实。02 的唯一主干、Protobuf-only、Hybrid 按对端扇出和核心/业务分层决策不变，无需新增选型。
- Commit：`33a0ef98006a188caafb9f84a20d97d1ddcd16e8 fix(quality): close MC-614 to MC-616 review gaps`。
- Push：质量修正提交已推送 `origin/development`；本纯台账证据 Commit 推送后再核验最终 `HEAD == origin/development`。当前任务仍为 MC-617 待开始。

### MC-613 质量评估修正记录

- 日期：2026-08-15
- 性质：用户授权的不占 MC 编号质量评估提交；不改变 MC-614 “待开始”，不提前实现业务 Proto 或 Demo。
- 状态：已完成。
- 待闭环：mainboard 单次信号等待竞态、RoutineFactory 首条消息丢失唤醒窗口、绑核失败可观测性与测试时序、`minicyber_91006` 测试残留、失效 Step/Phase 及与实现相反的注释。
- 验证要求：Debug 全量构建与 CTest 通过；mainboard 信号边界、RoutineFactory 首条消息、绑核、SHM 自清理和 MC-610~613 高风险链路完成定向回归。
- 原始失败证据：评估首次执行 `ctest --test-dir build/debug --output-on-failure` 退出码 8，44 项中 `test_pin_thread` 失败 1 项，本次运行失败率 1/1；关键输出为 `PinThreadTest.SetSchedAffinity1to1` 仍观察到非目标 CPU。同次测试后发现 `/dev/shm/minicyber_91006`。
- 修正结论：mainboard 在线程创建前通过 `pthread_sigmask` 阻塞信号，并由主线程以 `sigwait` 同步消费；RoutineWakeState 挂账任务号发布前通知；绑核返回系统调用结果并保持测试线程存活；SHM 测试自清理；`GetProtoConfig` 完成 TextFormat 加载；失效注释已按当前调用链修正。
- 真实验证：`cmake --build build/debug -j2` 退出码 0；`ctest --test-dir build/debug --output-on-failure` 为 44/44 通过；`ctest --test-dir build/debug --output-on-failure --repeat until-fail:10 -R '^test_(module_controller|routine_factory|pin_thread|posix_segment|component|scheduler_classic|scheduler_choreography)$'` 为 7/7 各连续 10 轮通过；修正前的五项直接相关测试另各连续 20 轮通过；`find /dev/shm -maxdepth 1 -name 'minicyber_*' -print` 无输出；`git diff --check` 通过。
- 文档同步：路线图仅增加局部门禁路由；01 将配置、竞态/资源测试和注释审计分别归入 MC-615/617/621；03 增加信号原子等待与绑核可观测口径；04/05 已同步质量产物。02 未发生架构选型变更，无需修改。
- 知识沉淀：`docs/DEBUG_MANUAL.md` 记录四类问题的风险时序、排查链、原因和回归；`docs/INTERVIEW_QA.md` 新增信号等待、首条唤醒和绑核成败问答。
- Commit：`da7366a05461e49440614cd54f785293a3b3b7da fix(quality): close MC-610 to MC-613 review gaps`。
- Push：已推送 `origin/development`；推送后核验该生产质量提交与当时远端 HEAD 一致。
- 下一步：仍为 MC-614，不得因本质量提交跳过或合并任务。

## 任务台账

### 第二次重构任务台账（当前）

| ID | 状态 | Commit | 验证/备注 |
|---|---|---|---|
| MC-601 | 已完成 | `2a8aaeb334cad929b7bbe75004c2b878b45ec93f docs(refactor): establish MC-601 agent baseline` | 根级红线、路线图冻结记录和 MC-602/603 交接清单已建立；Debug 配置和全量编译通过。 |
| MC-602 | 已完成 | `578e80d2f73f742f944febf7a23b391623ffa3fe docs(refactor): migrate MC-602 historical knowledge` | DEBUG/INTERVIEW 初稿已建立；离散笔记、UML、旧性能报告和原始数据已迁移后删除。 |
| MC-603 | 已完成 | `cc1c33a15f42fe1760addf9b070d0a0b4be8300e refactor: prune MC-603 legacy runtime surface` | 删除 RPC、Timer、旧示例、旧 benchmark、旧脚本与专属测试；Debug 全量构建、41 项 CTest 注册和黑名单扫描通过。 |
| MC-604 | 已完成 | `2126585f908580a5e15930c36c1480c73abbc2f1 refactor(build): establish MC-604 build boundary` | 显式生产/测试/Proto 清单、FastRTPS 系统门禁与 Scheduler Protobuf 骨架；Debug 配置和全量构建通过，已经 SSH 推送。 |
| MC-605 | 已完成 | `2119acbe12091b3fc20b4785320afc48e8bafcdf refactor(time): align MC-605 time primitives` | Time、Duration、Rate 分离至 `minicyber::time`；Debug 全量构建和 Time 单测连续 20 次通过。 |
| MC-606 | 已完成 | `5666133da94bd05b81254dd38c2ee799542a63f5 refactor(discovery): restore MC-606 channel control plane` | FastRTPS Participant 控制面与 ChannelManager 分层已建立；本次质量修正补齐原生编号/QoS、回环去重和确定性双向发现测试。 |
| MC-607 | 已完成 | `7b17a8a109b3b5635227ee3a35635b7acdb87dde refactor(transport): restore MC-607 hybrid fanout` | Protobuf SHM 与 INTRA/SHM 按对端扇出已验证；质量评估已补齐可重入关闭和段布局门禁。 |
| MC-608 | 已完成 | `063524ac43c3489be620fbf056fc42c2d6f46ea5 refactor(node): align MC-608 endpoint lifecycle` | RoleAttributes、Protobuf-only API、动态 HasReader 和 Disable -> Leave 已验证。 |
| MC-609 | 已完成 | `68db73a8a2756bd57f639d23b83cd76018ef7293 refactor(data): restore MC-609 routine factory chain` | 单/双输入 DataVisitor、AllLatest 和 DATA_WAIT 协程链已验证。 |
| MC-610 | 已完成 | `d88d142a3f9d7e5d64693c7650a9245f07c09d31 refactor(scheduler): restore MC-610 classic shared groups` | Classic 共享 20 级队列、Scheduler Protobuf 映射和 DataVisitor 唤醒已验证；已推送 `origin/development`。 |
| MC-611 | 已完成 | `1da8edde9856e4149ff225d4333f2db9a2f5b3f9 refactor(scheduler): restore MC-611 choreography dual zones` | Choreography 定向 Processor + Classic 公共池双区、任务回退和原路由唤醒已验证；已推送 `origin/development`。 |
| MC-612 | 已完成 | `f9607ad3d845f87a9c5c38132050b6204c51afc5 refactor(component): restore MC-612 coroutine execution` | 单/双通道 Component 已接回 Scheduler/CRoutine，删除同步 Proc；生产提交已推送 `origin/development`。 |
| MC-613 | 已完成 | `00b806d8310566529d281c8dad23cb9080ba1a13 refactor(mainboard): align MC-613 dynamic loading boundary` | mainboard 独立调度配置、强制 `.so + dlopen`、加载水位回滚和安全退出已验证；已推送 `origin/development`。 |
| MC-614 | 已完成 | `632c278bad4f40a66f7ef602cd8b91104c17861b feat(proto): define MC-614 autodrive message contract` | 六类业务消息、统一源序列/单调时间契约和 protoc/描述符测试已完成。 |
| MC-615 | 已完成 | `d3476034eafbf088db509d644bd1d53d6c4d76ca feat(autodrive): implement MC-615 component DAG` | 同一 DSO 的五组件、唯一 DAG、TextFormat 配置、确定性链路与真实卸载注销已验证。 |
| MC-616 | 已完成 | `bcff3079b5418090ce69208bf0b9b67ec69f58b8 feat(autodrive): implement MC-616 pipeline endpoints` | Source/Sink、拓扑 Join 放行、metrics、统一脚本和有序退出已验证并推送。 |
| MC-617 | 已完成 | `7c6e54a436be9766500b4766b8b6aacbcf1c243b test(runtime): harden MC-617 high-risk gates` | Debug 45/45、high_risk 21/21、ConditionNotifier 20 轮通过；已推送。 |
| MC-618 | 已完成 | 本任务单一提交 `test(integration): verify MC-618 classic pipeline` | Classic 1000 条/100 Hz、真实 dlopen、自然 SHM 回收与 3 轮 CTest 已验收。 |
| MC-619 | 待开始 | — | Choreography、混合扇出与 100% 留存生产文件触达。 |
| MC-620 | 待开始 | — | 仅使用唯一主干采集全新 CSV/JSON 并写中文性能报告。 |
| MC-621 | 待开始 | — | 编写最终 ARCHITECTURE 与自顶向下 CALL_CHAIN。 |
| MC-622 | 待开始 | — | 完成丰富的 DEBUG_MANUAL 与 INTERVIEW_QA。 |
| MC-623 | 待开始 | — | 最终功能/资源/文档/Git 验收，推送 development。 |

### MC-608 基线指针阻塞记录

- 日期：2026-08-14
- 任务：MC-608 对齐 Protobuf-only Node/Reader/Writer 与拓扑生命周期
- 状态：阻塞（已定位并修复）
- 原始证据：00 与 `MINICYBER_ROADMAP.md` 均指向 MC-608，但
  `docs/refactor/04_自校验定稿.md` 的当前任务一致性表和定稿结论仍指向 MC-607，
  `docs/refactor/05_实施归档.md` 的直接执行入口和“下一步”也仍指向 MC-607。
- 处理：00 是唯一状态事实源；本次仅同步上述历史指针，不改变任务卡、架构取舍或验收口径。
- 后续：指针一致后将 MC-608 标为“进行中”，再开始产品代码修改。

### MC-606 完成记录

- 日期：2026-08-14
- 任务：MC-606 恢复 FastRTPS Channel 发现控制面
- 状态：进行中，待生产 Commit 与 Push 证据收口
- 原生参考：`cyber_ref/cyber/proto/topology_change.proto`、
  `cyber_ref/cyber/service_discovery/topology_manager.{h,cc}`、
  `cyber_ref/cyber/service_discovery/specific_manager/channel_manager.{h,cc}`、
  `cyber_ref/cyber/service_discovery/communication/participant_listener.h`、
  `cyber_ref/cyber/transport/rtps/participant.{h,cc}`。
- 改动文件：`include/minicyber/proto/topology_change.proto`、
  `include/minicyber/service_discovery/channel_manager.h`、
  `include/minicyber/topology/topology_manager.h`、
  `src/service_discovery/channel_manager.cpp`、`src/topology/topology_manager.cpp`、
  `CMakeLists.txt`、`tests/CMakeLists.txt`、
  `tests/test_topology.cpp`、`tests/test_topology_discovery.cpp`、
  `docs/DEBUG_MANUAL.md`、`docs/INTERVIEW_QA.md`。
- 设计结论：ChannelManager 成为 Writer/Reader/Node 状态唯一所有者；TopologyManager
  只编排 Participant 控制消息和 ChangeListener 生命周期。仅接受同主机角色，维护
  对端快照、进程关系和 Join/Leave 变更连接。FastRTPS 只承载私有控制 Topic，
  Protobuf 以 DDS CDR 长度序列封装；未引入 RTPS 数据面类型或 HybridTransport。
- 质量修正：控制 Topic 对齐原生 `Reliable + Transient Local + Keep All(depth=10)`；过滤
  本进程回环消息；`scheduler_conf.proto` 与 `topology_change.proto` 恢复原生编号；
  跨进程测试通过管道确认 Reader 本地 Join 和双方观察到对端后再离场，不使用固定 sleep。
- 验证：
  `find /usr -iname '*fastrtps*config*.cmake' -print` 找到系统 Config；
  `cmake --build build/debug -j2` 成功；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:5 -R '^test_(topology|topology_discovery)$'`
  原提交通过（2/2 测试，连续 5 轮）；质量修正后 `test_topology_discovery` 连续 10 轮通过；`ctest -N -V` 确认发现测试带
  `unit;high_risk;cross_process`；RTPS 数据面黑名单及 `nm -CD` 符号扫描通过；
  `git diff --check` 通过。
- 资源检查：控制 Participant 在 Shutdown 前移除，离场监听删除远端角色；本任务不创建
  POSIX SHM 段，未发现新的 SHM 资源。
- 文档同步：已更新 `docs/DEBUG_MANUAL.md` 的 CDR 对齐故障、Participant 离场和回归路径，
  更新 `docs/INTERVIEW_QA.md` 的 FastRTPS 依赖与控制面事实。
- Commit：`refactor(discovery): restore MC-606 channel control plane`
- Push：已通过 SSH 推送 `origin/development`。
- 下一步：MC-607 恢复按对端关系扇出的 INTRA/SHM HybridTransport。

### MC-606 质量评估修正与基线同步

- 日期：2026-08-14
- 性质：用户授权的独立质量评估提交，不占用 MC 编号，不改变 MC-607“待开始”。
- 评审修正：恢复两个 Protobuf 的原生编号；补齐 ChannelManager 原生职责分层、控制 Topic
  QoS、回环去重、启动失败回滚、回调异常隔离和确定性双向跨进程测试；补充高风险意图注释。
- 基线同步：已同步 `AGENTS.md`、路线图和 00~05；新增原生映射、注释、知识增量、完整
  Debug 时间线、基线联动、完整 SHA 和五项交付证据门禁。
- 手册同步：DEBUG 记录本次发现时序问题的完整排查链；INTERVIEW 增加 ChannelManager
  分层、控制 QoS、回环过滤和 Protobuf 编号问答。
- 验证：Debug 全量构建通过；完整 CTest 为 42/43 通过，唯一失败仍是 MC-604 已登记且
  归属 MC-610 的 `ProcessorTest.WithClassicContextPriorityOrder` 时序断言，本次未越界修改
  Scheduler；`test_topology_discovery` 连续 10 轮通过。最终黑名单扫描仍命中
  `classic_context`、Scheduler 测试和相关头文件中的旧工作窃取实现/表述，这是 MC-610
  已明确登记的待删除范围，不在本次评估提交中提前改变调度行为。
- 01/02/03 检查：01、02、03 均已修改并形成任务共同门禁、架构事实和发现专项验收。
- Commit：`fix(discovery): close MC-606 quality review gaps`；该提交的完整 SHA 由提交后的
  `git log -1 --format='%H %s'` 唯一取得，Git Commit 无法在自身内容中保存自身 SHA。
- 下一步：仍为 MC-607，待开始。

### MC-607 完成记录

- 日期：2026-08-14
- 任务：MC-607 恢复 INTRA/SHM HybridTransport 扇出
- 状态：已完成
- 原生参考：`cyber_ref/cyber/transport/transmitter/hybrid_transmitter.h`、
  `cyber_ref/cyber/transport/receiver/hybrid_receiver.h`、
  `cyber_ref/cyber/transport/transport.h`、`cyber_ref/cyber/transport/transmitter/{intra_transmitter.h,shm_transmitter.h}`、
  `cyber_ref/cyber/transport/receiver/{intra_receiver.h,shm_receiver.h}`。
- 改动文件：`include/minicyber/transport/{transport.h,dispatcher/shm_dispatcher.h,transmitter/hybrid_transmitter.h,transmitter/shm_transmitter.h,receiver/hybrid_receiver.h,receiver/shm_receiver.h}`、
  `src/transport/dispatcher/shm_dispatcher.cpp`、`tests/{CMakeLists.txt,test_hybrid_transport.cpp,test_transport_routing.cpp}`、
  `docs/INTERVIEW_QA.md`、路线图与本轮基线文档。
- 设计结论：Hybrid 订阅 ChannelManager 的角色变更，按每个对端维护 `SAME_PROC -> INTRA` 与
  `DIFF_PROC -> SHM` 连接集合，同一 Writer 可并行扇出且每个 Reader 只由一个后端投递。
  Hybrid 的 Protobuf 模板以 `static_assert` 限定消息类型；SHM 只传序列化字节，接收端重建消息。
  不引入 RTPS 数据面、跨主机连接、History 或 MC-608 的 Node 生命周期改造。
- 验证：`cmake --build build/debug -j2` 成功；
  `ctest --test-dir build/debug --output-on-failure -R '^test_hybrid_transport$'` 通过；
  `test_hybrid_transport` 的真实 `fork` 子进程覆盖 Protobuf SHM 收发和动态 `bytes` 字段恢复；
  同一测试还覆盖零字节 Protobuf 的合法投递；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:5 -R '^test_(hybrid_transport|transport_routing|shm_receiver|shm_transmitter|topology|topology_discovery)$'`
  6/6 通过且每项连续 5 轮成功；最终完整 `ctest --test-dir build/debug --output-on-failure`
  为 43/44 通过，唯一失败是已登记且归属 MC-610 的
  `ProcessorTest.WithClassicContextPriorityOrder`（实际顺序 `100, 200`，期望 `200, 100`），
  本任务未越界修改 Classic 调度。
- 资源检查：Hybrid 测试在远端 Leave 后停用 SHM 分支并清理测试段；未发现 `/dev/shm/minicyber_*` 残留。
- 文档同步：`INTERVIEW_QA.md` 已增加 Hybrid per-opposite、Protobuf 字节边界和注销在途回调事实；
  未出现新的重大故障，不新增 `DEBUG_MANUAL.md` 时间线。
- 基线检查：Roadmap、00、04、05 已同步至 MC-608；01/02/03 已逐项检查，任务卡、架构取舍和验收口径均适用且无需修改。
- 代码注释：Hybrid 公开模板、连接集合、SHM 字节所有权和注销顺序均已写明原生职责与不变量。
- Commit：`7b17a8a109b3b5635227ee3a35635b7acdb87dde refactor(transport): restore MC-607 hybrid fanout`。
- Push：已推送 `origin/development`，质量评估时核验该提交位于远端历史。
- 下一步：MC-608 以 HybridTransport 为输入完成 Protobuf-only Node/Reader/Writer、HasReader 和拓扑生命周期。

### MC-608 完成记录

- 日期：2026-08-14
- 任务：MC-608 对齐 Protobuf-only Node/Reader/Writer 与拓扑生命周期
- 状态：已完成
- 前置输入：MC-607 的 `HybridTransmitter`/`HybridReceiver`、MC-606 的 ChannelManager
  与同机 FastRTPS Join/Leave 控制面。
- 原生参考：`cyber_ref/cyber/node/node.h`、
  `cyber_ref/cyber/node/node_channel_impl.h`、`cyber_ref/cyber/node/reader.h`、
  `cyber_ref/cyber/node/writer.h`。
- 改动文件：`include/minicyber/node/node.h`、
  `include/minicyber/node/node_channel_impl.h`、`include/minicyber/node/reader.h`、
  `include/minicyber/node/writer.h`、`tests/test_node.cpp`、
  `tests/test_component.cpp`、`tests/test_component_factory.cpp`、
  `tests/test_module_controller.cpp`、`tests/CMakeLists.txt`、`docs/INTERVIEW_QA.md`、
  `MINICYBER_ROADMAP.md`、`docs/refactor/00_进度记录.md`、
  `docs/refactor/04_自校验定稿.md`、`docs/refactor/05_实施归档.md`。
- 设计结论：NodeChannelImpl 为每个端点生成主机、进程、节点、Channel、消息类型、
  `FileDescriptorProto` 序列化字节与端点 ID 完整 RoleAttributes；Writer/Reader 模板以
  编译期断言限制 Protobuf 类型，创建 Hybrid 后 Join ChannelManager。关闭按
  `Disable -> Leave`，避免 Leave 触发的动态变更重新接通正在关闭的端点。Reader 仅保留
  有界 Observe 队列和用户回调，不提前实现 MC-609/MC-612 的 DataVisitor、RoutineFactory
  或同步业务 Proc。
- 验证：`cmake --build build/debug -j2` 成功；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:5 -L high_risk`
  4/4 通过且每项连续 5 轮成功，覆盖 `test_node`、`test_hybrid_transport`、
  `test_topology`、`test_topology_discovery`；`ctest -N -V -R '^test_node$'` 确认
  标签为 `unit;high_risk`。最终一次完整
  `ctest --test-dir build/debug --output-on-failure` 实际为 42/44 通过：已登记且归属
  MC-610 的 `ProcessorTest.WithClassicContextPriorityOrder` 仍失败（实际顺序
  `100, 200`，期望 `200, 100`）；`ConditionNotifierTest.ForkCrossProcessNotify` 有一次
  子进程状态 1 的未稳定复现失败。随后该 ConditionNotifier 测试连续 5 轮通过，故本任务
  不越界修改其跨进程实现；MC-608 高风险集连续 5 轮仍全部通过。
- 资源检查：动态 Join/Leave、Reader 关闭后 Writer 无对端和 Node 关闭后外部持有端点
  不可再通信均由 `test_node` 覆盖；高风险回归未发现新的 FastRTPS Participant 或 POSIX
  SHM 残留。
- 文档同步：`docs/INTERVIEW_QA.md` 已增加 RoleAttributes 共同连接键、发现就绪门禁和
  `Disable -> Leave` 生命周期问答；本次没有新的重大故障、泄漏、死锁或 ABI 根因，
  `docs/DEBUG_MANUAL.md` 不适用且未修改。
- 基线检查：Roadmap、00、04、05 已指向 MC-609；01 的 MC-608 任务卡已按原范围完成，
  02 的 Protobuf-only/Hybrid/无 RTPS 数据面决策适用，03 的动态发现、生命周期和高风险
  验收口径适用，三者均无需修改。
- 代码注释：Node、NodeChannelImpl、Writer、Reader 的公开边界、RoleAttributes 所有权、
  初始化回滚、关闭顺序和 MC-609/MC-612 责任边界均已写明原生 CyberRT 对应职责。
- Commit：`063524ac43c3489be620fbf056fc42c2d6f46ea5 refactor(node): align MC-608 endpoint lifecycle`。
- Push：已推送 `origin/development`，质量评估时核验该提交位于远端历史。
- 下一步：MC-609 恢复单/双通道 DataVisitor、AllLatest 与 RoutineFactory 数据驱动链。

### MC-609 完成记录

- 日期：2026-08-14
- 任务：MC-609 恢复 DataVisitor 与 RoutineFactory 数据驱动链
- 状态：已完成
- 前置输入：MC-608 的 Protobuf-only Reader 有界队列与安全端点关闭，及既有
  DataDispatcher/DataNotifier 的快照分发和在途注销屏障。
- 原生参考：`cyber_ref/cyber/data/data_visitor.h`、
  `cyber_ref/cyber/data/fusion/all_latest.h`、`cyber_ref/cyber/croutine/routine_factory.h`。
- 改动文件：`include/minicyber/croutine/croutine.h`、
  `include/minicyber/croutine/routine_factory.h`、
  `include/minicyber/data/data_visitor_base.h`、`include/minicyber/data/data_visitor.h`、
  `include/minicyber/data/fusion/data_fusion.h`、`include/minicyber/data/fusion/all_latest.h`、
  `tests/test_data_visitor.cpp`、`tests/test_data_fusion.cpp`、
  `tests/test_routine_factory.cpp`、`tests/CMakeLists.txt`、`docs/INTERVIEW_QA.md`、
  `MINICYBER_ROADMAP.md`、`docs/refactor/00_进度记录.md`、
  `docs/refactor/04_自校验定稿.md`、`docs/refactor/05_实施归档.md`。
- 设计结论：DataVisitor 只保留单/双输入 ChannelBuffer、TryFetch、游标和 notifier；
  AllLatest 仅在主通道 Fill 时复用副通道最新值并先填融合队列。RoutineFactory 仅生成
  单/双输入 CRoutine 循环，按 `DATA_WAIT -> TryFetch -> Proc -> Yield(READY)` 执行，
  并把 DataVisitorBase 交给后续 Scheduler 注册 NotifyTask。删除三/四输入偏特化和
  DataVisitor 直接 Fetch，避免发布线程同步业务消费。
- 验证：`cmake --build build/debug -j2` 成功；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:10 -R '^test_(data_visitor|data_fusion|fusion_all_latest|routine_factory|croutine)$'`
  5/5 通过且每项连续 10 轮成功；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:5 -L high_risk`
  11/11 通过且每项连续 5 轮成功。完整
  `ctest --test-dir build/debug --output-on-failure` 实际为 45/45 通过；此前已登记、归属
  MC-610 的 `ProcessorTest.WithClassicContextPriorityOrder` 时序失败本轮未复现，本任务未越界
  修改 Classic。
- 资源检查：RoutineFactory 回归验证 Proc 在 Processor 线程而非发布线程执行；
  DataVisitor 析构先注销 notifier 并等待在途回调，再注销 Buffer；未创建 RTPS 数据面、
  进程或 POSIX SHM 资源。
- 文档同步：`docs/INTERVIEW_QA.md` 已增加 RoutineFactory、主通道 AllLatest 和
  单/双输入边界问答；未出现新的重大 Bug、泄漏、死锁、ABI 或生命周期根因，
  `docs/DEBUG_MANUAL.md` 不适用且未修改。
- 基线检查：Roadmap、00、04、05 已指向 MC-610；01 的 MC-609 任务卡已按原范围完成，
  02 的单/双输入与发布线程隔离决策适用，03 的 DATA_WAIT、游标单次推进和填充先于唤醒
  验收口径适用，三者均无需修改。
- 代码注释：DataVisitorBase、DataVisitor、AllLatest、RoutineFactory 和
  CRoutine::GetCurrentRoutine 已说明原生职责、游标所有权、融合/唤醒顺序和冻结栈所有权。
- Commit：`68db73a8a2756bd57f639d23b83cd76018ef7293 refactor(data): restore MC-609 routine factory chain`。
- Push：已推送 `origin/development`，质量评估时核验该提交与当时远端 HEAD 一致。
- 下一步：MC-610 以 DataVisitorBase 和 RoutineFactory 为输入恢复 Classic 共享调度组。

### MC-610 完成记录

- 日期：2026-08-14
- 任务：MC-610 恢复 Classic 共享调度组语义
- 状态：已完成
- 前置输入：MC-609 的 `DataVisitorBase`、`RoutineFactory` 与
  `DATA_WAIT -> READY` 数据协程链。
- 原生参考：`cyber_ref/cyber/scheduler/policy/{scheduler_classic.h,scheduler_classic.cc,classic_context.h,classic_context.cc}`。
- 改动文件：`include/minicyber/scheduler/{scheduler.h,scheduler_conf.h,policy/classic_context.h,policy/choreography_context.h}`、
  `src/scheduler/{scheduler.cpp,policy/classic_context.cpp}`、
  `tests/{CMakeLists.txt,test_classic_context.cpp,test_routine_factory.cpp,test_scheduler_classic.cpp}`、
  `docs/INTERVIEW_QA.md`、`MINICYBER_ROADMAP.md`、
  `docs/refactor/{00_进度记录,04_自校验定稿,05_实施归档}.md`。
- 设计结论：`SchedulerConf::FromProto` 仅转换本任务接收的 Classic group、
  processor_num、task priority 与 affinity 字段；同组 Processor 各自持有
  `ClassicContext`，但共享同名组的静态 20 级队列和等待条件。具名任务按
  ClassicTask 路由，未知任务进入第一个组；`Acquire` 只防止同一 CRoutine 并发
  Resume。`Scheduler::CreateTask(RoutineFactory)` 注册 DataVisitor 回调到
  NotifyTask，Shutdown 先断开回调状态再销毁任务和 Processor，避免悬垂回调。
  未修改 MC-611 的 Choreography 路由，不引入 RTPS 数据面。
- 验证：`cmake --build build/debug -j2 --target test_scheduler_classic test_classic_context test_processor test_routine_factory` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:20 -R '^test_(scheduler_classic|classic_context|processor|routine_factory)$'`
  为 4/4 通过且每项连续 20 轮成功；`cmake --build build/debug -j2 && ctest --test-dir build/debug --output-on-failure`
  退出码 0，44 项注册测试全部通过；`ctest --test-dir build/debug --output-on-failure -L high_risk`
  为 13/13 通过；生产 Scheduler 和相关测试边界扫描无 `work-stealing`、`Steal` 或
  `proc_数字` 私有组命名，`git diff --check` 通过。
- 资源检查：本任务仅启动测试线程；所有 Scheduler 均调用 Shutdown，未创建进程、动态库
  或 POSIX SHM 资源，未观察到异常退出或非预期失败。
- 文档同步：`docs/INTERVIEW_QA.md` 已增加 Classic 共享组、Acquire、数据唤醒与关闭
  顺序问答；未出现需建立时间线的新重大故障，`docs/DEBUG_MANUAL.md` 不适用且未修改。
- 基线检查：Roadmap、00、04、05 已同步指向 MC-611；01 的 MC-610 任务卡已按原范围完成；
  02 的 Classic 共享队列和无工作窃取决策适用；03 的 Classic、DATA_WAIT、关闭重建与
  high_risk 验收口径适用，01/02/03 均无需修改。
- 代码注释：SchedulerConf 映射、共享组、RoutineFactory 回调所有权和 Shutdown 顺序均已
  标注原生职责与不变量；ClassicContext 删除旧私有队列表述。
- Commit：`d88d142a3f9d7e5d64693c7650a9245f07c09d31 refactor(scheduler): restore MC-610 classic shared groups`。
- Push：已推送 `origin/development`，当前 `HEAD == origin/development`。
- 下一步：MC-611 以 Classic 共享组作为公共池基础，恢复 Choreography 定向区。

### MC-611 完成记录

- 日期：2026-08-14
- 任务：MC-611 恢复 Choreography 定向区与 Classic 公共池
- 状态：已完成
- 前置输入：MC-610 的 Classic 共享 20 级队列、Scheduler Protobuf 映射和
  MC-609 的 `DataVisitorBase -> Scheduler::NotifyTask` 唤醒链。
- 原生参考：`cyber_ref/cyber/scheduler/policy/{scheduler_choreography.h,scheduler_choreography.cc,choreography_context.h,choreography_context.cc}`、`cyber_ref/cyber/scheduler/scheduler_factory.cc`。
- 改动文件：`include/minicyber/scheduler/{scheduler.h,scheduler_conf.h}`、
  `src/scheduler/{scheduler.cpp,scheduler_factory.cpp}`、
  `tests/{CMakeLists.txt,test_scheduler_choreography.cpp,test_scheduler_classic.cpp}`、
  `docs/{DEBUG_MANUAL.md,INTERVIEW_QA.md}`、`MINICYBER_ROADMAP.md`、
  `docs/refactor/{00_进度记录,04_自校验定稿,05_实施归档}.md`。
- 设计结论：按 `choreography_processor_num` 创建 Choreography 定向 Processor，按
  `pool_processor_num` 创建 Classic 公共池；显式有效 `processor_id` 定向入队，未配置
  或越界任务进入默认 Classic 共享多级队列。数据通知依据 CRoutine 保存的原路由唤醒
  定向 Context 或公共组；空任一区域在 Proto/Factory 入口拒绝，未知策略不静默降级。
  未引入 RTPS 数据面，也未改变 MC-610 Classic 共享组语义。
- 验证：`cmake --build build/debug -j2 --target test_scheduler_choreography test_scheduler_classic` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure -R '^test_(scheduler_choreography|scheduler_classic)$'` 为 2/2 通过；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:20 -R '^test_(scheduler_choreography|scheduler_classic|processor|classic_context|routine_factory)$'`
  为 5/5 测试各连续 20 轮通过（100/100）；`cmake --build build/debug -j2` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure` 为 44/44 通过；`git diff --check` 通过。
- 资源检查：测试仅启动本进程 Scheduler 线程；所有 Scheduler 均执行 Shutdown，未创建
  动态库或跨进程资源，未观察到异常退出和 SHM 残留。
- 文档同步：`docs/INTERVIEW_QA.md` 增加双区路由、公共池回退和结构 ABI 问答；
  `docs/DEBUG_MANUAL.md` 记录一次旧测试对象 ABI 不一致导致的 `stack smashing`（退出码
  8，1/1），完整重编译后 `test_routine_factory` 连续 20/20 通过，归属 MC-611 构建/ABI 收口。
- 基线检查：Roadmap、00、04、05 已同步指向 MC-612；01 的 MC-611 任务卡按原范围完成；
  02 的 Classic 公共池、定向 Processor 和无 RTPS 数据面决策适用；03 的双区、原路由唤醒
  和空区域拒绝门禁适用；01/02/03 均无需修改，processor_id 越界回公共池遵循 MC-611
  任务卡局部门禁。
- 代码注释：新增配置字段、Factory 拒绝边界、双区创建、任务路由、NotifyTask 原路由和
  Shutdown 公共组清理均已说明原生职责、不变量与所有权；未逐行翻译实现。
- Commit：`1da8edde9856e4149ff225d4333f2db9a2f5b3f9 refactor(scheduler): restore MC-611 choreography dual zones`。
- Push：生产提交已推送 `origin/development`；纯台账证据提交后再次核验 `HEAD == origin/development`。
- 下一步：MC-612 接收双区 Scheduler 与原路由唤醒，接回 Component 协程执行模型。

### MC-612 完成记录

- 日期：2026-08-14
- 任务：MC-612 恢复单/双通道 Component 协程执行模型
- 状态：已完成
- 前置输入：MC-608 的 Protobuf-only Node/Reader 生命周期、MC-609 的单/双输入
  DataVisitor/RoutineFactory，以及 MC-611 的双区 Scheduler 与原路由唤醒。
- 原生参考：`cyber_ref/cyber/component/{component_base.h,component.h}`。
- 改动文件：`include/minicyber/component/{component.h,component_base.h,component_factory.h}`、
  `src/component/component_factory.cpp`、`include/minicyber/{scheduler/scheduler.h,data/{data_dispatcher.h,data_notifier.h,data_visitor.h},transport/receiver/shm_receiver.h}`、
  `src/scheduler/{scheduler.cpp,policy/{classic_context.cpp,choreography_context.cpp}}`、
  `tests/{CMakeLists.txt,test_component.cpp,test_component_factory.cpp,test_module_controller.cpp}`、
  `CMakeLists.txt`、双手册及路线图/基线文档。
- 设计结论：Component 仅保留单/双 Protobuf 输入；Initialize 先执行业务 Init 和无同步
  callback 的 Reader 创建，再创建 DataVisitor、RoutineFactory 并提交 Scheduler。双输入首
  Reader 是 AllLatest 主驱动。ComponentBase 关闭固定为禁止新 Proc、RemoveTask 解除
  Visitor 唤醒、注销 Reader、Clear 释放业务资源、关闭 Node；当前 Proc 自关闭不等待自身
  Acquire。SHM 解码后的副本仅投递 DataVisitor，避免回流到 IntraReceiver 造成 Hybrid
  重复投递。ComponentFactory 单例定义移入核心库，为 MC-613 的真实 dlopen 提供唯一表。
- 验证：`cmake --build build/debug -j2` 退出码 0；稳定构建后
  `ctest --test-dir build/debug --output-on-failure` 为 44/44 通过；
  `ctest --test-dir build/debug --output-on-failure -L high_risk` 为 15/15 通过；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:5 -R '^(test_hybrid_transport|test_component)$'`
  为 2/2 各连续 5 轮通过。此前 `test_module_controller` 同步 Proc 旧断言首次失败（退出码
  8，1/4，失败率 1/1）和 Hybrid SHM 回流首次失败（退出码 8，1/8，失败率 1/1）均已按
  完整时间线登记 DEBUG；修复后稳定全量集通过。一次构建与 CTest 并发导致的 6 项异常
  不作为产品结论，已在无并发构建的稳定产物上复验。
- 资源检查：测试创建的 `/dev/shm/minicyber_91006` 已按 `test_posix_segment.cpp` 的精确
  Channel 所有权清理；最终 `find /dev/shm -maxdepth 1 -name 'minicyber_*' -print` 无输出。
- 文档同步：`docs/INTERVIEW_QA.md` 已增加 Component 协程、关闭顺序和唯一工厂问答；
  `docs/DEBUG_MANUAL.md` 已完整记录旧同步断言和 SHM 回流误投递的复现、排查、根因与回归。
- 基线检查：路线图、00、04、05 已同步指向 MC-613；01 的 MC-612 任务卡已按原范围完成；
  02 的单/双输入、首输入 AllLatest、Protobuf-only 与无同步 Proc 决策适用；03 的线程
  归属、关闭和 Hybrid 无重复投递验收口径适用。01/02/03 均无需修改。
- 代码注释：Component 装配、Scheduler RemoveTask、ComponentBase 关闭、SHM 数据协程
  分流与 ComponentFactory 跨 DSO 单例均说明原生职责、所有权与关键不变量。
- Commit：`f9607ad3d845f87a9c5c38132050b6204c51afc5 refactor(component): restore MC-612 coroutine execution`。
- Push：已推送 `origin/development`；纯台账证据提交后再次核验 `HEAD == origin/development`。
- 下一步：MC-613 接收 Component 协程模型，完成 mainboard 独立 Scheduler 配置和真实
  `.so + dlopen` 边界。

### MC-613 完成记录

- 日期：2026-08-14
- 任务：MC-613 对齐 mainboard、调度参数与动态库边界
- 状态：已完成
- 前置输入：MC-612 的单/双 Protobuf Component 协程执行、唯一 ComponentFactory 与
  ComponentBase 关闭顺序；MC-610/611 的 Classic/Choreography SchedulerFactory。
- 原生参考：`cyber_ref/cyber/mainboard/mainboard.cc`、
  `cyber_ref/cyber/mainboard/module_argument.{h,cc}`、
  `cyber_ref/cyber/mainboard/module_controller.cc`。
- 改动文件：`bin/mainboard.cpp`、`src/mainboard/module_controller.cpp`、
  `include/minicyber/mainboard/module_controller.h`、
  `include/minicyber/component/component_factory.h`、
  `src/component/component_factory.cpp`、`CMakeLists.txt`、
  `tests/{CMakeLists.txt,module_controller_test_plugin.cpp,test_module_controller.cpp}`、
  `docs/{DEBUG_MANUAL.md,INTERVIEW_QA.md}` 与路线图/00/04/05 指针。
- 设计结论：mainboard 只接受唯一 `-d` 与 `-s`，先解析 Scheduler Protobuf 并由
  SchedulerFactory 创建策略，再加载 DAG；ModuleController 强制非空 `module_library`，
  只使用 `dlopen(RTLD_NOW | RTLD_GLOBAL)`。失败回滚仅覆盖本次加载水位，严格为逆序
  Shutdown、销毁组件、`dlclose`；插件注册器卸载时注销 CreatorFunc。信号处理器只写
  `sig_atomic_t`，主线程按组件、Transport/Discovery、Scheduler 顺序清理。
- 验证：`cmake -S . -B build/debug -DCMAKE_BUILD_TYPE=Debug && cmake --build build/debug -j2 --target mainboard test_module_controller` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure -R '^test_module_controller$'` 通过；
  `ctest --test-dir build/debug --output-on-failure -R '^test_(module_controller|component|component_factory|scheduler_classic|scheduler_choreography)$' --repeat until-fail:5`
  为 5 项各连续 5 轮通过；`cmake --build build/debug -j2 && ctest --test-dir build/debug --output-on-failure`
  为 Debug 全量构建成功、44/44 通过。测试真实 `.so` 注册/创建、空库拒绝、未知类水位
  回滚、`I -> S -> D -> U` 卸载顺序，以及同一 DAG 配 Classic/Choreography 后 SIGTERM 有界退出。
- 资源检查：两种 mainboard 子进程均退出码 0；全量 CTest 后发现既有
  `test_posix_segment.CloseIsIdempotent` 固定通道 `minicyber_91006`（该测试按设计只
  Close 不 Destroy），以 `ctest --test-dir build/debug --output-on-failure -R '^test_posix_segment$'`
  复核退出码 0、确认无进程持有后仅删除该显式孤立段；随后
  `find /dev/shm -maxdepth 1 -name 'minicyber_*' -print` 无输出。
- 文档同步：`docs/INTERVIEW_QA.md` 已增加独立 Scheduler 配置、动态库卸载顺序和信号
  边界问答；`docs/DEBUG_MANUAL.md` 已增加失效 CreatorFunc 的风险、修复和回归记录。
- 基线检查：路线图、00、04、05 已同步指向 MC-614；01 的 MC-613 任务卡按原范围完成；
  02 的同一 DAG/非空库/真实 dlopen 决策适用，03 的加载、回滚、关闭和信号验收口径适用；
  01/02/03 无需修改。
- 代码注释：mainboard 信号边界、Scheduler 先决条件、ModuleController 回滚水位和
  ComponentFactory 跨 DSO 注销均说明了 CyberRT 职责和所有权不变量。
- Commit：`00b806d8310566529d281c8dad23cb9080ba1a13 refactor(mainboard): align MC-613 dynamic loading boundary`。
- Push：已推送 `origin/development`；推送后 `HEAD == origin/development == 00b806d8310566529d281c8dad23cb9080ba1a13`。
- 下一步：MC-614 仅定义自动驾驶业务 Protobuf 的时间戳与序列契约，不提前创建业务组件或 DAG。

### MC-614 完成记录

- 日期：2026-08-15
- 任务：MC-614 定义自动驾驶业务 Protobuf 与指标字段
- 状态：已完成。
- 前置输入：MC-605 的 `time::Time::MonoTime()` 单调时钟职责，以及 MC-613 的显式
  Protobuf 构建边界。
- 原生参考：任务卡指定的 `cyber_ref/examples/proto/examples.proto` 在当前参考树不存在；
  已核对同目录实际存在的 `cyber_ref/examples/proto/header.proto`、`chatter.proto`、
  `driver.proto`，以 Header 的序列字段 1、时间字段 2 职责作为唯一替代依据。
- 改动文件：`include/minicyber/proto/autodrive.proto`、`CMakeLists.txt`、
  `tests/{CMakeLists.txt,test_autodrive_proto.cpp}`、`docs/INTERVIEW_QA.md` 与路线图/00/04/05
  指针。
- 设计结论：CameraFrame、VehicleState、PerceptionObstacle、FusedObstacle、Trajectory 与
  ControlCommand 均将字段 1、2 固定为 `source_sequence`、`source_monotonic_ns`；测量序列
  从 1 单调递增，0 仅为 VehicleState 预热标记且不进入丢包集合。源时间来自
  `CLOCK_MONOTONIC`，后续组件只透传；其余字段仅覆盖确定性障碍、轨迹与控制量计算，未提前
  实现组件、DAG、进程入口或指标采集。
- 验证：`cmake -S . -B build/debug` 退出码 0；
  `cmake --build build/debug -j2 --target test_autodrive_proto` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure -R '^test_autodrive_proto$'` 为 1/1 通过；
  `cmake --build build/debug -j2` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure` 为 45/45 通过。定向测试同时断言六个
  生成类型均派生 `google::protobuf::Message`、字段 1/2 的名称和 `uint64` 类型，以及序列化
  后源关联键保持不变。
- 资源检查：`find /dev/shm -maxdepth 1 -name 'minicyber_*' -print` 无输出；本任务未创建
  进程、线程或 SHM 段。
- 文档同步：`docs/INTERVIEW_QA.md` 已新增业务消息源关联键与统计边界问答；无重大 Bug、
  ABI、生命周期或选型变更，`docs/DEBUG_MANUAL.md` 不适用。
- 基线检查：路线图、00、04、05 已同步推进至 MC-615；01 的 MC-614 任务卡按原范围完成；
  02 的 Protobuf-only、轻量确定性业务字段决策适用；03 的源字段透传、预热和丢包口径适用；
  01/02/03 均无需修改。
- 代码注释：`autodrive.proto` 说明字段 1/2 的编号不变量、预热边界、单调时钟来源和对齐的
  CyberRT 示例职责；未逐行复述生成语法。
- Commit：`632c278bad4f40a66f7ef602cd8b91104c17861b feat(proto): define MC-614 autodrive message contract`。
- Push：生产提交已推送 `origin/development`；推送后核验
  `HEAD == origin/development == 632c278bad4f40a66f7ef602cd8b91104c17861b`。本次纯台账证据
  提交完成后再次核验远端同步。
- 下一步：MC-615 接收稳定消息契约，实现五个业务组件共享库和唯一 DAG；不得更改本任务字段
  编号或提前实现 MC-616 的 Source、Sink 与 metrics。

### MC-615 完成记录

- 日期：2026-08-15
- 任务：MC-615 实现唯一业务组件共享库与 DAG
- 状态：已完成。
- 前置输入：MC-614 的六类 `autodrive.proto` 消息、字段 1/2 的源序列/单调时间契约，以及
  MC-612 的单/双 Component 协程模型和 MC-613 的真实 `dlopen` 生命周期边界。
- 原生参考：`cyber_ref/examples/cyber/common_component_example/common_component_example.{h,cc}`；
  任务卡省略了实际参考树中的 `cyber/` 路径前缀，已按存在文件核对双输入 Component 的
  `Init/Proc` 职责与注册边界。
- 改动文件：`demo/autodrive/components/*.cpp`、`config/autodrive/{autodrive.dag,components/*.conf}`、
  `include/minicyber/proto/autodrive_runtime.h`、`src/data/data_notifier.cpp`、`demo/autodrive/autodrive_runtime.cpp`、
  `include/minicyber/{data/data_notifier.h,mainboard/module_controller.h}`、`CMakeLists.txt`、
  `tests/{CMakeLists.txt,test_autodrive_components.cpp}`、`docs/{DEBUG_MANUAL.md,INTERVIEW_QA.md}`
  与路线图/00/04/05 指针。
- 设计结论：Perception、Fusion、Planning、Control、ControlAudit 注册到同一个业务 `.so`；
  Fusion 保留首通道 PerceptionObstacle 驱动和 VehicleState AllLatest 融合，其他组件为单通道，
  Writer 全在 Init 创建、Proc 仅做确定性转换和发布。唯一 DAG 的五份 TextFormat 配置均经
  `ComponentBase::GetProtoConfig` 读取。业务 Dispatcher、Notifier 和输出消息控制块由
  `minicyber_core` 唯一持有，插件使用 `extern template`；业务插件以 `-fno-gnu-unique` 允许
  `Clear -> dlclose` 执行注册器析构，不改变原有 ComponentFactory 或 Scheduler 职责。
- 失败证据：同一命令
  `ctest --test-dir build/debug --output-on-failure -R '^test_autodrive_components$'` 曾连续四次
  非预期失败，均为单项失败率 1/1、CTest 退出码 8：第 1 次为 `autodrive.proto` 重复注册
  `GeneratedDatabase()->Add`；第 2 次为跨 DSO DataDispatcher 分裂导致 2 秒链路超时；第 3 次为
  GNU unique 使插件 `NODELETE`、`Clear()` 后工厂条目未注销；第 4 次在真实卸载后收到 SIGSEGV，
  `gdb` 定位为仍由插件生成的 ControlCommand `shared_ptr` 控制块。完整复现、工具、排除证据、
  修复边界和回归已登记 `docs/DEBUG_MANUAL.md` 的两个 MC-615 条目；后续归属已在本任务以
  核心运行时所有权收口，无遗留转交。
- 验证：`cmake --build build/debug -j2 --target test_autodrive_components` 退出码 0；定向
  `ctest --test-dir build/debug --output-on-failure -R '^test_autodrive_components$'` 为 1/1 通过；
  同命令加 `--repeat until-fail:5` 为 1/1 连续 5 轮通过（5/5）；
  `cmake --build build/debug -j2` 退出码 0；`ctest --test-dir build/debug --output-on-failure`
  为 46/46 通过；`git diff --check` 通过。
- 资源检查：五个组件在单进程 Scheduler 内退出，`ModuleController::Clear()` 后组件计数、库
  计数和工厂注册均归零；`find /dev/shm -maxdepth 1 -name 'minicyber_*' -print` 无输出。
- 文档同步：`docs/DEBUG_MANUAL.md` 完整登记 Protobuf 描述符、数据总线、GNU unique 与消息
  控制块的调试时间线；`docs/INTERVIEW_QA.md` 增加静态 Protobuf ABI 和可卸载插件运行时所有权
  问答。
- 基线检查：Roadmap、00、04、05 已同步指向 MC-616；01 的 MC-615 任务卡已按原范围完成；
  02 的同一业务 DSO、唯一 DAG、Protobuf-only 和不恢复 RTPS 数据面决策适用；03 的反射加载、
  确定性数据链与资源验收口径适用；01/02/03 均无需修改。
- 代码注释：DAG、跨 DSO Dispatcher/Notifier、输出消息控制块、插件卸载和 ComponentBase 关闭
  顺序均说明 CyberRT 对应职责、所有权和不变量；组件算法只注释字段透传与 AllLatest 的业务边界。
- Commit：`d3476034eafbf088db509d644bd1d53d6c4d76ca feat(autodrive): implement MC-615 component DAG`。
- Push：生产提交已推送 `origin/development`，推送后核验
  `d3476034eafbf088db509d644bd1d53d6c4d76ca == origin/development`；本纯台账证据提交推送后再次
  核验最终 `HEAD == origin/development`。
- 下一步：MC-616 只消费本任务唯一业务 `.so` 与 DAG，实现 Source、Sink、统一启动和指标采集；
  不修改本任务组件算法、DAG 或跨 DSO 卸载边界。

### MC-616 实施记录

- 日期：2026-08-15
- 任务：MC-616 实现 SensorSource、ControlSink、统一启动与指标采集。
- 状态：已完成。
- 前置输入：MC-615 的唯一业务 `.so`、唯一 `autodrive.dag`、五组件确定性数据链，以及 MC-614
  固定的 `source_sequence` / `source_monotonic_ns` 契约。
- 原生参考：`cyber_ref/examples/cyber/talker.cc` 的独立 Node 与 `time::Rate` 节拍职责，
  `cyber_ref/cyber/time/rate.{h,cc}` 的周期修正语义；本任务仅复用既有 Node/Writer/Reader 和
  Channel Join/Leave，不扩展 RTPS 数据面或复制第二份 DAG。
- 产物：新增 `sensor_source`、`control_sink`、Classic 调度配置和唯一启动脚本；Source 等待两个
  输入 Reader，先发 VehicleState 预热并经过一个 Rate 周期后按 VehicleState -> CameraFrame
  顺序发布。Sink 以 ControlCommand Writer Join 写入有界 ready 条件，脚本据此在启动 Source 前
  放行；Sink 统计分位数、吞吐、缺号和乱序，完成后以 Sink 全序列确认排空并回收三个进程与四个
  精确 SHM 频道。裸 DSO 名称交给 `LD_LIBRARY_PATH` 解析；mainboard 用 `sigwait` 消费正常退出
  信号，避免异步 SHM Join 覆盖 handler 后把 SIGTERM 误作崩溃。
- 非预期失败记录：首次 10 条运行因裸库名被拼接 CWD 而 `dlopen` 失败，脚本退出码 3、失败率
  1/1；加载修复后默认运行先后出现 `994/1000`、`978/1000` 的缺号，均为 Sink 退出码 3、各自
  失败率 1/1；下游 Join 门禁首次成功收齐数据时 mainboard 因 SIGTERM handler 被 SHM 异步创建
  覆盖而退出 143、失败率 1/1。复现命令、原始日志、候选排除、根因、修复边界和回归已完整登记
  `docs/DEBUG_MANUAL.md` 的“MC-616 多进程主干启动与尾部排空”；没有将这些失败以连续复跑通过
  覆盖。
- 验证：`cmake --build build/debug -j2` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure` 为 46/46 通过；两次
  `scripts/run_autodrive_pipeline.sh --messages 1000 --frequency 100 --timeout-ms 30000`
  均退出 0，分别得到 `received=1000 missing=0 out_of_order=0`，吞吐为 100.119 与 99.837
  msg/s；`find /dev/shm -maxdepth 1 -name 'minicyber_*' -print` 无输出；`git diff --check` 通过。
- 文档同步：`docs/DEBUG_MANUAL.md` 已记录完整多进程失败时间线；`docs/INTERVIEW_QA.md` 已新增
  AllLatest 预热/发布顺序、Source 排空和完整拓扑 Join 放行问答。
- 基线检查：路线图、00、04、05 已同步指向 MC-617 并更新本任务产物状态；
  01 的 MC-616 卡已按原范围实现，02 的唯一主干、Protobuf-only、Hybrid 按对端路由及无 RTPS
  数据面决策适用，03 的 1000 条/100 Hz、拓扑 Join、指标、优雅退出和 SHM 无残留口径适用；
  01/02/03 均无需修改。
- Commit：`bcff3079b5418090ce69208bf0b9b67ec69f58b8 feat(autodrive): implement MC-616 pipeline endpoints`。
- Push：生产提交已推送 `origin/development`；推送后核验
  `bcff3079b5418090ce69208bf0b9b67ec69f58b8 == origin/development`。本纯台账证据提交推送后
  再次核验最终 `HEAD == origin/development`。
- 下一步：MC-617 只收敛任务卡指定的高风险单测；不得提前实施 Classic/Choreography 业务验收。

### MC-617 执行记录（提交前）

- 日期：2026-08-15
- 任务：MC-617 收敛高风险单元测试。
- 状态：已完成。
- 上一步输入：MC-616 唯一 Source/Sink、统一启动脚本、Classic 配置和真实多进程失败记录。
- 原生参考：`cyber_ref/cyber/croutine`、`cyber_ref/cyber/transport/shm/condition_notifier.*`、
  `cyber_ref/cyber/scheduler`、`cyber_ref/cyber/component/component.h`。
- 改动文件：`CMakeLists.txt`、`demo/autodrive/control_sink.cpp`、
  `demo/autodrive/metrics.{h,cpp}`、`tests/CMakeLists.txt`、
  `tests/test_condition_notifier.cpp`、`tests/test_control_sink_metrics.cpp`、
  `docs/DEBUG_MANUAL.md`、`docs/INTERVIEW_QA.md`。
- 设计结论：核心 Proto 与自动驾驶 Proto 按 ABI 所有权拆分；业务 Runtime 持有六类业务消息
  描述符和模板实例；metrics 关闭时只保留序列集合，不保留延迟样本；跨进程通知测试改为
  管道/有界条件，固定 key 的启动观察窗口先排空；CTest 标签收敛为 `unit`、`high_risk`、
  `cross_process`、`integration`。
- 验证：`cmake --build build/debug -j2` 退出码 0；`ctest --test-dir build/debug --output-on-failure`
  45/45 通过；`ctest --test-dir build/debug --output-on-failure -L high_risk` 21/21 通过；
  `ctest --test-dir build/debug --output-on-failure --repeat until-fail:20 -R '^test_condition_notifier$'`
  20/20 通过；首次全量失败 `ConditionNotifierTest.ListenTimeout` 为 1/47、CTest 退出码 8，
  已按规则登记 `docs/DEBUG_MANUAL.md`；核心 `nm -D` 扫描无自动驾驶消息符号，Runtime 扫描命中。
- 资源检查：高风险与全量 CTest 结束后未观察到本任务新增 POSIX SHM 残留；测试自行回收精确
  SysV/POSIX 资源。
- 文档同步：`DEBUG_MANUAL.md` 已登记首次失败完整时间线与回归；`INTERVIEW_QA.md` 已补充
  核心/业务 Proto ABI 和 metrics 开关问答；01/02/03 适用性检查通过且无需修改。
- 基线检查：路线图、00、04、05 已同步指向 MC-618；01 的 MC-617 卡已按范围交付，02 的
  Protobuf-only、核心/业务分层和原生 ConditionNotifier 决策、03 的高风险和资源口径均适用，
  01/02/03 无需修改。
- Commit：`7c6e54a436be9766500b4766b8b6aacbcf1c243b test(runtime): harden MC-617 high-risk gates`。
- Push：生产提交已推送；提交后核验
  `7c6e54a436be9766500b4766b8b6aacbcf1c243b == origin/development`。
- 下一步：MC-618 仅执行 Classic 的唯一业务 DAG 集成验收。

### MC-618 执行记录（提交前）

- 日期：2026-08-15
- 任务：MC-618 完成 Classic 业务主干集成验收。
- 状态：已完成。
- 上一步输入：MC-617 的 Debug 45/45、high_risk 21/21、ConditionNotifier 失败记录、
  Protobuf-only SHM 边界和统一三进程脚本。
- 原生参考：`cyber_ref/cyber/scheduler/policy/scheduler_classic.cc`、
  `cyber_ref/cyber/transport/shm/{shm_conf.cc,segment.cc}`、
  `cyber_ref/cyber/transport/transmitter/shm_transmitter.h`。
- 改动文件：`include/minicyber/transport/shm/posix_segment.h`、
  `include/minicyber/transport/{dispatcher/shm_dispatcher.h,transmitter/shm_transmitter.h}`、
  `tests/{CMakeLists.txt,run_autodrive_classic_pipeline.sh,test_hybrid_transport.cpp}`、
  `docs/{DEBUG_MANUAL.md,INTERVIEW_QA.md}`、路线图与 00/04/05。
- 设计结论：业务视角 CTest 只复用唯一 DAG/脚本，验证真实 dlopen、Classic 共享组、
  进程间 SHM、组件内 INTRA、AllLatest 预热、Processor/CRoutine 前置单测、1000 条
  Control/Audit 集合和四频道自然回收。正式 SHM Reader/Writer 默认池对齐原生首档
  16 KiB/512 块，消除 4 块循环覆盖未读通知；不恢复动态分档或复制业务管道。
- 原始失败：首次 CTest 因构建目录工作目录导致 TextFormat 相对路径失效（退出码 8，1/1）；
  第二次因正常空 Sink 日志的错误非空断言失败（退出码 8，1/1）；随后高风险集出现真实
  `994/998` 业务尾部缺失、重复和乱序（退出码 8，21/22）。三次均记录在
  `DEBUG_MANUAL.md`，每次 SHM 精确检查退出码均为 0。
- 验证：`cmake --build build/debug -j2` 退出码 0；主干脚本
  `scripts/run_autodrive_pipeline.sh --messages 1000 --frequency 100 --metrics` 退出码 0，
  `received=1000 audit_received=1000 lost=0 duplicates=0 audit_duplicates=0 out_of_order=0 audit_difference=0`；
  Classic CTest 连续 3/3 通过；high_risk 22/22 通过；Debug 全量 CTest 46/46 通过。
- 资源检查：所有成功路径均在任何兜底 `shm_unlink` 前检查 Camera、VehicleState、
  ControlCommand、ControlAudit 四个精确 POSIX SHM 名称自然消失；失败现场和逐进程日志保留在
  `build/debug/autodrive-classic-ctest-*`，未观察残留进程或 SHM 名称。
- 文档同步：`DEBUG_MANUAL.md` 已登记三次失败、根因、修复和回归；`INTERVIEW_QA.md` 已更新
  原生 SHM 首档容量问答。01 的任务卡按范围执行；02 的 Classic、Hybrid、Protobuf-only 决策和
  03 的完整主干、自然回收口径均适用，01/02/03 无需修改。
- 基线检查：路线图、00、04、05 已同步指向 MC-619；Classic 产物已登记，MC-619 的
  Choreography、混合扇出和文件触达仍未实施。
- Commit：本任务单一提交 `test(integration): verify MC-618 classic pipeline`，完整 SHA 以 Git 历史为准。
- Push：提交后核验 `HEAD == origin/development`。
- 下一步：MC-619 只使用同一 DAG 和 Choreography 配置验收混合扇出及文件触达。

### MC-607 至 MC-609 质量评估修正记录

- 日期：2026-08-14
- 性质：用户授权的不占 MC 编号质量评估提交；不改变 MC-610“待开始”，不提前实现
  Classic 共享组、Component 协程化或业务 Demo。
- 原生参考：`cyber_ref/cyber/transport/{transmitter/hybrid_transmitter.h,receiver/hybrid_receiver.h}`、
  `cyber_ref/cyber/transport/shm/{segment.cc,shm_conf.cc}`。
- 评审修正：Hybrid 改为锁内维护期望状态、锁外同步分发和启停后端，增加回调内关闭
  transmitter/receiver 回归；当时正式 Protobuf SHM 两端统一 64 KiB/4 块，段打开失败或既存
  容量不足时初始化失败；该容量在 MC-618 主干中证实会覆盖未读通知，现已恢复为原生首档
  16 KiB/512 块；删除旧固定 INTRA、非 Protobuf Transport 工厂和专属离散测试；
  Processor 优先级测试先建立完整候选集再启动工作线程，Snapshot 测试等待其真实后置条件。
- 未越界项：Classic 生产调度语义仍由 MC-610 接收；Component 同步路径和旧阶段注释仍由
  MC-612 清理；ConditionNotifier 偶发跨进程失败已进入 DEBUG，归属 MC-617。
- 定向验证：`test_processor` 连续 20 轮通过并已纳入 `high_risk`；
  `test_hybrid_transport` 连续 10 轮通过，覆盖回调内关闭、2048 字节动态字段、零字节消息、
  混合扇出和不兼容既存段拒绝；Node、PosixSegment、RoutineFactory 联合定向测试通过。
- 全量验证：`cmake --build build/debug -j2` 退出码 0；
  `ctest --test-dir build/debug --output-on-failure` 为 44/44 通过；全量运行前清理了无进程
  占用的 `/dev/shm/minicyber_*` 历史残留，Hybrid 回归后未产生新的该类残留。
- 文档同步：Roadmap/AGENTS 只增加偶发失败登记和纯台账证据规则；具体门禁分散至
  MC-607/608/610/612/617/619/623 任务卡；02/03/04/05、DEBUG_MANUAL 和 INTERVIEW_QA
  已同步本次事实。
- Commit：`638d854bea8765e2661e90eff8a2adf4fde9d250 fix(quality): close MC-607 to MC-609 review gaps`。
- Push：已推送 `origin/development`，且质量证据 Commit 后核验 `HEAD == origin/development`。
- 下一步：MC-610 待开始，接收稳定的 Processor 测试前置与既有 DataVisitorBase。

### 第一次重构历史台账（MC-001~MC-504）

| ID | 状态 | Commit | 验证/备注 |
|---|---|---|---|
| MC-001 | 已完成 | `docs(refactor): record MC-001 baseline` | Debug 配置成功；构建因 `src/swap.S` 的 GNU as `--defsym` 参数失败；CTest 注册 47，均 Not Run。详见 `baseline.md`。 |
| MC-002 | 已完成 | `docs(refactor): map MC-002 modules` | 已建立逐模块映射、偏差、范围外能力及 MC-003 至 MC-006 删除候选；人工路径/引用检查通过。详见 `module_mapping.md`。 |
| MC-003 | 已完成（合并清理） | `refactor: remove legacy runtime stack` | 删除根级 Fiber 实现、桥接与专属测试；将仍需的协程上下文迁入 `croutine/detail`。 |
| MC-004 | 已完成（合并清理） | `refactor: remove legacy runtime stack` | 删除根级 Context；保留 `src/swap.S`，由新 `croutine/detail/routine_context` 使用。 |
| MC-005 | 已完成（合并清理） | `refactor: remove legacy runtime stack` | 删除根级 IOManager、epoll 测试和旧示例。 |
| MC-006 | 已完成（合并清理） | `refactor: remove legacy runtime stack` | 删除根级 Thread/旧 Scheduler 及专属测试；保留新 `scheduler/**`。 |
| MC-101 | 已完成 | `refactor(croutine): align MC-101 lifecycle` | Resume 仅执行 READY、返回状态；Stop 延迟生效；补齐 Wake/HangUp/Sleep 与完成语义测试。 |
| MC-102 | 已完成 | `refactor(croutine): align MC-102 context switch` | 对齐 x86_64 保存寄存器布局、栈对齐和汇编元数据；新增双向上下文切换测试。 |
| MC-103 | 已完成 | `refactor(scheduler): align MC-103 processor loop` | 对齐任务取得、执行后的 Release、空队列等待与停止路径；补齐快照协程名和锁释放测试。 |
| MC-104 | 已完成 | `refactor(scheduler): align MC-104 classic context` | 对齐本地优先级队列、Acquire/Release 和移除等待语义，移除非原生跨组窃取。 |
| MC-105 | 已完成 | `refactor(scheduler): align MC-105 classic lifecycle` | 对齐 Classic Processor 创建、CPU 亲和性、调度策略配置与关闭后拒绝提交语义。 |
| MC-106 | 已完成 | `refactor(scheduler): align MC-106 choreography context` | 对齐本地优先级队列、Acquire/Release、等待和并发删除语义。 |
| MC-107 | 已完成 | `refactor(scheduler): add MC-107 choreography routing` | 新增策略工厂、Choreography Processor/Context 启动、目标处理器路由与定向唤醒。 |
| MC-108 | 已完成 | `refactor(scheduler): stabilize shutdown submission` | 调度关闭、并发提交和策略切换稳定性测试；新增 CTest `scheduler;stability;tsan` 标签与 `check_scheduler_tsan` 入口。 |
| MC-201 | 已完成 | `test(data): verify MC-201 buffer semantics` | 对齐并验证有效容量、满覆盖、绝对索引读取、游标溢出快进及批量读取顺序。 |
| MC-202 | 已完成 | `refactor(data): synchronize dispatcher lifecycle` | 增加显式 buffer 注销；以安全快照保护注册、分发与并发生命周期。 |
| MC-203 | 已完成 | `refactor(data): synchronize notifier lifecycle` | 增加 notifier/visitor 显式注销、回调快照和 in-flight 回调同步，覆盖 DATA_WAIT 唤醒。 |
| MC-204 | 已完成 | `refactor(data): align all-latest fusion` | 恢复 primary Fill 融合钩子，补齐 2/3/4 输入 DataFusion、AllLatest 与 DataVisitor 语义。 |
| MC-205 | 已完成 | `refactor(transport): align intra lifecycle` | INTRA dispatcher/收发器重复 20 轮通过；路由回归通过。 |
| MC-206 | 已完成 | `refactor(shm): align segment block ownership` | block/state/segment/posix 测试连续 20 轮通过；SHM 收发回归通过。 |
| MC-207 | 已完成 | `refactor(shm): harden posix segment lifecycle` | POSIX 与关联 SHM 测试连续 10 轮通过。 |
| MC-208 | 已完成 | `refactor(shm): synchronize condition notifier` | notifier 连续 20 轮通过；SHM dispatcher/收发回归通过。 |
| MC-209 | 已完成 | `refactor(transport): synchronize shm lifecycle` | SHM dispatcher/收发器连续 10 轮通过；路由回归通过。 |
| MC-301 | 已完成 | `refactor(transport): stabilize local route selection` | 自动工厂固定 INTRA，SHM 保持显式接口；路由/拓扑测试连续 10 轮通过。 |
| MC-302 | 已完成 | `refactor(node): align endpoint ownership` | 引入精简 NodeChannelImpl；Node 显式、幂等关闭所有受管端点。 |
| MC-303 | 已完成 | `refactor(node): add reader history semantics` | 回调、默认深度 1 的有界历史及 Observe/ClearData 通过链路回归验证。 |
| MC-304 | 已完成 | `refactor(component): harden lifecycle cleanup` | 失败初始化回收端点；Component/Timer 生命周期和重试回归通过。 |
| MC-305 | 已完成 | `refactor(component): harden factory error paths` | 拒绝无效注册，创建器锁外执行；工厂/模块控制器回归通过。 |
| MC-306 | 已完成 | `refactor(mainboard): validate DAG proto inputs` | 对齐必要 proto 字段；DAG Reader 深度传递与解析校验回归通过。 |
| MC-307 | 已完成 | `refactor(mainboard): rollback failed DAG loads` | 组件/库加载按尝试范围回滚；mainboard 失败路径关闭 Scheduler。 |
| MC-401 | 已完成 | `build: add configuration presets` | Debug/Release/ASAN/TSAN 预设均可生成；Release 不再含默认 `-O0 -g`。 |
| MC-402 | 已完成 | `test: classify CTest execution paths` | `unit/stress/cross_process/stability/sanitizer` 标签与专项入口已建立。 |
| MC-403 | 已完成 | `perf: align intra and pipe latency benchmark` | INTRA/PIPE 均为 SPSC 单向延迟，统一载荷、预热、样本和 CSV 输出。 |
| MC-404 | 已完成 | `perf: add cross-process shm benchmark collection` | SHM 双进程 SPSC 延迟基准与统一 CSV/JSON 采集脚本。 |
| MC-405 | 已完成 | `perf: add mutex condition queue baseline` | 容量 1 的 SPSC 互斥锁+条件变量队列纳入统一采集。 |
| MC-406 | 已完成 | `perf: document cpp20 coroutine experiment` | 独立 C++20 编译/恢复探针；因无同口径执行器而排除性能比较。 |
| MC-407 | 已完成 | `perf: publish reproducible performance report` | Release 原始 CSV/JSON、环境清单、报告与 SHM 基准回收验证已归档。 |
| MC-501 | 已完成 | `docs: align README with verified capabilities` | README 仅说明有测试/数据证据的范围、构建与性能结论。 |
| MC-502 | 已完成 | `docs: archive implementation decisions and lessons` | 建立最终架构、迭代、问题、限制和复现导航。 |
| MC-503 | 已完成 | `test: complete final validation and sanitizer cleanup` | Debug/Release/ASAN 全量验收通过；清理 ASAN 发现的问题。 |
| MC-504 | 已完成 | `docs: record development branch release state` | `development` 历史、工作区边界与远端推送状态已核对。 |

## 更新模板

```text
日期：YYYY-MM-DD
任务：MC-xxx
状态：进行中/已完成/阻塞
原生参考：cyber_ref/...
改动文件：...
设计结论：...
验证：命令；结果
资源检查：进程/线程/SHM/动态库；结果或不适用
文档同步：DEBUG_MANUAL/INTERVIEW_QA；结果或不适用原因
基线检查：Roadmap/00/04/05 同步结果；01/02/03 修改或不适用原因
代码注释：公开 API、协议、并发、所有权、回滚和关闭顺序检查结果
Commit：<完整 40 位 SHA> <subject>
Push：development 远端状态
阻塞或后续：...
```

## MC-603 冻结留存清单

下列清单是 MC-603 完成时仍受构建管理的精确源文件和测试文件，也是 MC-604 改为显式 CMake 源列表时唯一可接收的输入。Proto 生成物位于构建目录，不计入源码清单。

```text
bin/mainboard.cpp
include/minicyber/base/{atomic_hash_map.h,atomic_rw_lock.h,bounded_queue.h,macros.h,rw_lock_guard.h,wait_strategy.h}
include/minicyber/common/types.h
include/minicyber/component/{component.h,component_base.h,component_factory.h}
include/minicyber/croutine/{croutine.h,detail/routine_context.h}
include/minicyber/data/{cache_buffer.h,channel_buffer.h,data_dispatcher.h,data_notifier.h,data_visitor.h,data_visitor_base.h,fusion/all_latest.h,fusion/data_fusion.h}
include/minicyber/mainboard/module_controller.h
include/minicyber/node/{node.h,node_channel_impl.h,reader.h,writer.h}
include/minicyber/proto/{component_conf.proto,dag_conf.proto,qos_profile.proto,role_attributes.proto}
include/minicyber/scheduler/{common/pin_thread.h,policy/choreography_context.h,policy/classic_context.h,processor.h,processor_context.h,scheduler.h,scheduler_conf.h,scheduler_factory.h}
include/minicyber/time/time.h
include/minicyber/topology/topology_manager.h
include/minicyber/transport/{common/identity.h,dispatcher/intra_dispatcher.h,dispatcher/shm_dispatcher.h,message/message_info.h,receiver/intra_receiver.h,receiver/receiver.h,receiver/shm_receiver.h,shm/block.h,shm/condition_notifier.h,shm/posix_segment.h,shm/segment.h,shm/state.h,transmitter/intra_transmitter.h,transmitter/shm_transmitter.h,transmitter/transmitter.h,transport.h}
src/{croutine.cpp,croutine/detail/routine_context.cpp,mainboard/module_controller.cpp,scheduler/common/pin_thread.cpp,scheduler/policy/choreography_context.cpp,scheduler/policy/classic_context.cpp,scheduler/processor.cpp,scheduler/processor_context.cpp,scheduler/scheduler.cpp,scheduler/scheduler_factory.cpp,swap.S,topology/topology_manager.cpp,transport/dispatcher/shm_dispatcher.cpp,transport/shm/condition_notifier.cpp,transport/shm/posix_segment.cpp}
tests/CMakeLists.txt
tests/{test_atomic_hash_map.cpp,test_atomic_rw_lock.cpp,test_base_macros.cpp,test_bounded_queue.cpp,test_cache_buffer.cpp,test_channel_buffer.cpp,test_choreography_context.cpp,test_classic_context.cpp,test_common_types.cpp,test_component.cpp,test_component_base.cpp,test_component_factory.cpp,test_condition_notifier.cpp,test_croutine.cpp,test_data_dispatcher.cpp,test_data_fusion.cpp,test_data_notifier.cpp,test_data_visitor.cpp,test_fusion_all_latest.cpp,test_intra_dispatcher.cpp,test_intra_receiver.cpp,test_intra_transmitter.cpp,test_module_controller.cpp,test_node.cpp,test_pin_thread.cpp,test_posix_segment.cpp,test_posix_segment_signal.cpp,test_processor.cpp,test_processor_context.cpp,test_scheduler_choreography.cpp,test_scheduler_classic.cpp,test_shm_block.cpp,test_shm_dispatcher.cpp,test_shm_receiver.cpp,test_shm_segment.cpp,test_shm_state.cpp,test_shm_transmitter.cpp,test_smoke.cpp,test_topology.cpp,test_transport_routing.cpp,test_wait_strategy.cpp}
```

### MC-604 至 MC-609 留存增量

下列文件是任务卡按白名单新增并进入显式构建的生产/测试文件。它们与 MC-603 冻结清单
共同构成 MC-610 的实际输入和 MC-619 文件触达分母：

```text
include/minicyber/proto/{scheduler_conf.proto,topology_change.proto}
include/minicyber/service_discovery/channel_manager.h
include/minicyber/time/{duration.h,rate.h}
include/minicyber/transport/{receiver/hybrid_receiver.h,transmitter/hybrid_transmitter.h}
include/minicyber/croutine/routine_factory.h
src/service_discovery/channel_manager.cpp
src/time/{duration.cpp,rate.cpp,time.cpp}
tests/{test_hybrid_transport.cpp,test_routine_factory.cpp,test_time.cpp,test_topology_discovery.cpp}
```

质量评估已删除 `tests/test_transport_routing.cpp`：该测试只验证本轮已废弃的固定 INTRA
Transport 工厂，不进入 MC-619 最终触达或测试分母。

## 历史记录

日期：2026-08-14
任务：MC-605
状态：已完成
原生参考：`cyber_ref/cyber/time/{time.h,time.cc,duration.h,duration.cc,rate.h,rate.cc}`。
改动文件：`include/minicyber/time/{time.h,duration.h,rate.h}`、`src/time/{time.cpp,duration.cpp,rate.cpp}`、`tests/test_time.cpp`、`CMakeLists.txt`、`tests/CMakeLists.txt`、`docs/DEBUG_MANUAL.md`、`docs/refactor/00_进度记录.md`。
设计结论：按 CyberRT 职责将 Time、Duration、Rate 分离，保留墙钟 `Now`、单调 `MonoTime`、`SleepUntil`、纳秒间隔算术和 Rate 超期后的周期重置。时间类型置于 `minicyber::time`，避免与协程内部 `minicyber::Duration` 别名重名；不改变 Scheduler 睡眠语义，不引入 Timer 或 TimerComponent。
验证：`cmake --preset debug -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/tmp/minicyber-googletest2 && cmake --build build/debug -j2 --target test_time` 退出码 0；随后 `cmake --build build/debug -j2` 退出码 0，全量构建通过。`ctest --test-dir build/debug --output-on-failure -R '^test_time$' --repeat until-fail:20` 为 1 项连续 20 次通过，覆盖 Duration 算术、协程/时间头文件共存、MonoTime、SleepUntil、Rate 超期和 Reset。`ctest --test-dir build/debug -N` 注册 42 项测试，`git diff --check` 通过。
资源检查：未启动业务进程、线程、SHM 或动态库；不适用。
文档同步：已更新 `docs/DEBUG_MANUAL.md`，记录 CRoutine 与时间库 Duration 冲突的症状、根因、修复边界和回归命令；INTERVIEW_QA 不适用，本任务未产生新的架构选型。
Commit：`refactor(time): align MC-605 time primitives`
Push：`development` 已通过 SSH 推送并与 `origin/development` 同步。
阻塞或下一步：MC-606 接收 FastRTPS 构建门禁和时间库；只恢复 Channel Join/Leave 发现控制面，不得引入 RTPS 数据面。

日期：2026-08-14
任务：MC-604
状态：已完成
原生参考：`cyber_ref/cyber/proto/{cyber_conf.proto,scheduler_conf.proto,classic_conf.proto,choreography_conf.proto}`、`cyber_ref/third_party/fastrtps/fastrtps.BUILD`。
改动文件：`CMakeLists.txt`、`tests/CMakeLists.txt`、`include/minicyber/proto/scheduler_conf.proto`、`docs/refactor/00_进度记录.md`。
设计结论：生产源、Proto 源和测试源均固定为 MC-603 留存清单，禁止 GLOB 扩大边界。FastRTPS 以 `find_package(FastRTPS REQUIRED CONFIG NAMES FastRTPS fastrtps)` 作为系统门禁；本机 Debian 包由 `/usr/lib/x86_64-linux-gnu/cmake/fastrtps/fastrtps-config.cmake` 导出，实际链接 target 为 `fastrtps`。精简 `scheduler_conf.proto` 保留原生 `InnerThread`、Classic group/task、Choreography 定向区/公共池及线程亲和职责，未提前解析或实现后续 Scheduler 行为。
验证：`find /usr -iname '*fastrtps*config*.cmake'` 找到 `/usr/lib/x86_64-linux-gnu/cmake/fastrtps/fastrtps-config.cmake`；`cmake --preset debug -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/tmp/minicyber-googletest2 && cmake --build build/debug -j2` 退出码 0，Proto 生成和全量构建通过；独立构建目录确认 `FastRTPS_DIR=/usr/lib/x86_64-linux-gnu/cmake/fastrtps`，链接命令包含 `/usr/lib/x86_64-linux-gnu/libfastrtps.so.2.5.0`。`ctest --test-dir build/mc604-debug --output-on-failure -R '^test_(croutine|module_controller|node|scheduler_classic|scheduler_choreography)$'` 为 5/5 通过。全量 CTest 为 40/41 通过，唯一失败是未改动的 `test_processor.WithClassicContextPriorityOrder` 时序断言；该 Scheduler 优先级行为属于 MC-610，未在 MC-604 越界修改。`rg 'file\(GLOB' CMakeLists.txt tests/CMakeLists.txt` 无命中，`git diff --check` 通过。
资源检查：未启动业务进程、线程、SHM 或动态库；不适用。
文档同步：DEBUG_MANUAL/INTERVIEW_QA 不适用；本任务只建立构建和配置骨架，未产生新的重大 Bug、生命周期或选型事实。
Commit：`refactor(build): establish MC-604 build boundary`
Push：首次 HTTPS 推送因缺少凭据失败，随后改用 SSH 推送 `development`，并确认与 `origin/development` 同步。
阻塞或下一步：MC-605 接收显式构建边界及生成的 Scheduler Protobuf；应仅对齐 Time、Duration、Rate。

日期：2026-08-14
任务：MC-604
状态：阻塞
原生参考：`cyber_ref/cyber/proto/{cyber_conf.proto,scheduler_conf.proto}`、`cyber_ref/third_party/fastrtps/fastrtps.BUILD`。
改动文件：`docs/refactor/00_进度记录.md`。
设计结论：MC-604 必须使用系统 `find_package(FastRTPS REQUIRED)`，不得以 FetchContent、下载镜像或复制 `cyber_ref` 源码绕过；在该系统前置满足前，不能声称构建骨架可发现 FastRTPS，也不能进入 MC-605。
验证：`cmake --find-package -DNAME=FastRTPS -DCOMPILER_ID=GNU -DLANGUAGE=CXX -DMODE=EXIST` 退出码 1，输出 `FastRTPS not found.`；`pkg-config --modversion fastrtps` 与 `pkg-config --modversion fastdds` 均退出码 1；`ldconfig -p` 和 `/usr`、`/usr/local` 的 `FastRTPSConfig.cmake`、`fastrtpsConfig.cmake`、`fastdds-config.cmake` 扫描均无结果。
资源检查：未启动业务进程、线程、SHM 或动态库；不适用。
文档同步：DEBUG_MANUAL/INTERVIEW_QA 不适用；本次仅确认既有系统依赖缺失，未产生新的项目 Bug、生命周期或选型事实。
Commit：未创建；阻塞任务未达到任务卡的构建产出与最小验证门槛。
Push：未推送；无可完成的 MC-604 提交。
阻塞或下一步：需要提供系统 FastRTPS CMake Config Package，且必须能由 `find_package(FastRTPS REQUIRED)` 发现并给出实际导出 target 名；满足后从 MC-604 继续。

日期：2026-08-14
任务：MC-603
状态：已完成
原生参考：本任务只执行 `docs/refactor/01_实施方案.md` 已定稿的删除边界；保留文件分别由后续 MC-604 的显式构建、MC-605 的时间、MC-606 的拓扑和 MC-609 的数据链任务接收。
改动文件：删除 `include/minicyber/service/**`、`include/minicyber/component/timer_component.h`、四个旧示例、`scripts/collect_benchmark.sh` 与 RPC/Timer 专属测试；修改 Node、ComponentBase、Component 配置/DAG Proto、ModuleController、CMake 和受影响的剩余测试注释。
设计结论：不保留自研 RPC、TimerComponent、定时 DAG 配置或旧 benchmark 入口；Node 不再暴露 RPC 工厂。`base/time/topology/data` 审计后均保留，因为其职责分别由当前主干或 MC-605、MC-606、MC-609 明确接收；零输入和三/四输入路径、旧 TopologyManager 仍由后续任务处理，未提前删除。
验证：`cmake --preset debug && cmake --build build/debug -j2`；退出码 0，Debug 重新配置和全量构建通过。`ctest --test-dir build/debug -N`；退出码 0，注册 41 项且不含已删除的 RPC/Timer 测试。`ctest --test-dir build/debug --output-on-failure -R '^test_(common_types|component|component_base|module_controller|node)$'`；退出码 0，5/5 通过。对工作区源码执行 RPC、Timer、旧示例、旧 benchmark、旧脚本和相关 API 的黑名单 `rg` 扫描；无命中。`git diff --check` 通过。
资源检查：纯代码与构建入口清理；未启动业务进程、线程或 SHM，不适用。
文档同步：DEBUG_MANUAL/INTERVIEW_QA 不适用；删除取舍已由 MC-602 按 02/03 定稿迁入双手册，本任务未产生新的重大 Bug、生命周期或选型事实。
Commit：`refactor: prune MC-603 legacy runtime surface`
Push：`development` 已推送并与 `origin/development` 同步。
阻塞或后续：MC-604 必须以本节留存清单改为显式构建源列表，加入系统 FastRTPS 门禁和 Scheduler Protobuf 骨架；不得恢复本任务删除的路径。

日期：2026-08-14
任务：MC-602
状态：已完成
原生参考：`cyber_ref/cyber/{croutine,scheduler,data,transport,component,mainboard}` 的历史职责映射；当前架构与验收以 `docs/refactor/02_架构取舍矩阵.md`、`docs/refactor/03_验收与性能口径.md` 为准。
改动文件：`docs/DEBUG_MANUAL.md`、`docs/INTERVIEW_QA.md`、`MINICYBER_ROADMAP.md`、`docs/refactor/{00_进度记录,04_自校验定稿,05_实施归档}.md`；删除 `docs/` 下基线组以外的离散笔记、UML 和 `docs/refactor/` 下旧基线/性能资料。
设计结论：将 x86_64 栈对齐、MainFunc 栈冻结 shared_ptr 泄漏、Notifier 安全注销、AllLatest 先填充后唤醒、POSIX SHM 信号清理、调度关闭和动态加载回滚迁入双手册。Classic、Choreography、INTRA/SHM、Protobuf、FastRTPS 控制面和 `.so + dlopen` 仅按 02/03 的终局边界表述；旧 PIPE、互斥队列、C++20 探针和性能数值不迁移。
验证：`cmake --build build/debug -j2 --target test_croutine test_posix_segment_signal && ctest --test-dir build/debug --output-on-failure -R '^test_(croutine|posix_segment_signal)$'`；退出码 0，2/2 通过。`find docs -type f -print | sort` 仅保留两份手册和 `00_` 至 `05_` 八个文件；`git diff --check` 通过。旧路径检索仅命中任务卡、路线图迁移记录和首轮历史记录；`README.md` 的旧性能报告链接由 MC-623 收口，未越过本任务所有权修改。
资源检查：纯文档迁移与删除任务；未启动业务进程、线程或 SHM，不适用。
文档同步：已新增 DEBUG_MANUAL/INTERVIEW_QA 初稿；MC-622 在真实完整主干证据形成后扩写定稿。
Commit：`docs(refactor): migrate MC-602 historical knowledge`
Push：`development` 已推送并与 `origin/development` 同步。
阻塞或后续：MC-603 可按冻结白名单清理代码和测试；不得恢复已删除旧文档或性能口径。MC-623 必须删除 README 中指向已删除旧性能报告的链接并按唯一业务主干重写性能说明。

日期：2026-08-14
任务：MC-601
状态：已完成
原生参考：`cyber_ref/cyber/component/component.h`、`cyber_ref/cyber/scheduler/scheduler_factory.cc`、`cyber_ref/cyber/transport/transport.h`、`cyber_ref/cyber/service_discovery/topology_manager.h`
改动文件：`AGENTS.md`、`MINICYBER_ROADMAP.md`、`docs/refactor/00_进度记录.md`
设计结论：建立根级七条接力红线；冻结首轮代码的构建、依赖和生产文件基线。当前工程仍以递归 GLOB 编入 `src/**` 与全部 Proto，存在 RPC、TimerComponent、旧本地拓扑、同步 Component、旧示例和旧性能入口；这些分别由 MC-602/MC-603 迁移或清理，MC-601 不提前修改产品代码。FastRTPS 仅作为 MC-604 的系统依赖前置，当前未发现其 CMake Config Package。
验证：`cmake --find-package -DNAME=FastRTPS -DCOMPILER_ID=GNU -DLANGUAGE=CXX -DMODE=EXIST`；退出码 1，输出 `FastRTPS not found.`。`cmake --preset debug -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/tmp/minicyber-googletest2 && cmake --build build/debug -j2`；退出码 0，Debug 配置与全量构建通过。
资源检查：纯文档和基线任务；未启动业务进程、线程或 SHM，不适用。
文档同步：DEBUG_MANUAL/INTERVIEW_QA 不适用；本任务未修复重大 Bug、未改变生命周期或架构选型。MC-602 负责从旧资料建立两份手册初稿。
Commit：`docs(refactor): establish MC-601 agent baseline`
Push：`development` 已推送并与 `origin/development` 同步。
阻塞或后续：MC-602 已具备冻结的迁移/删除清单；必须先迁移有效历史知识，再删除旧离散文档与性能资料。

日期：2026-08-14
任务：第二次重构文档基线同步（实施前准备，不占用 MC 编号）
状态：已完成
原生参考：`cyber_ref/cyber/{croutine,scheduler,data,transport,node,component,mainboard,service_discovery,time}`
改动文件：`MINICYBER_ROADMAP.md`、`docs/refactor/00_进度记录.md` 至 `05_实施归档.md`
设计结论：统一 MC-601~MC-623 的需求、架构、验收和接力基线；MC-001~MC-504 降为历史起点。Timer/RPC/旧 benchmark 删除，FastRTPS 仅作同机发现控制面，数据面仅 INTRA/SHM，唯一自动驾驶主干作为功能与性能入口。
验证：路线图与 00~05 共七份基线文件交叉检查任务范围、关键决策、默认负载、文档链接和当前状态；结果一致。实施方案 23 张任务卡均具备固定四项字段。
资源检查：纯文档修改，不适用。
文档同步：本次建立的是 00~05 基线；DEBUG_MANUAL/INTERVIEW_QA 由 MC-602 迁移初稿，未提前伪造。
Commit：本次基线提交（以 `git log -1 --oneline` 获取）
Push：统一推送 `development`。
阻塞或后续：当前任务为 MC-601，状态待开始。系统 FastRTPS CMake Package 尚未检测到，属于 MC-604 前置风险，不阻塞 MC-601~MC-603。

日期：2026-08-13
任务：MC-504
状态：已完成
原生参考：无
改动文件：`docs/refactor/00_进度记录.md`
验证：`git branch --show-current` 为 `development`；`git log --oneline` 显示 MC-401 至 MC-503 按任务粒度提交，MC-003 至 MC-006 为用户授权的唯一合并清理；`git status --short` 中仅保留用户原有 `MINICYBER_ROADMAP.md` 与 `docs/refactor/01_` 至 `03_` 草稿改动。远端 `origin` 指向 `https://github.com/LiuXuanlings/minicyber.git`，无既有 `development` 分支；HTTPS 推送缺少凭据，使用已配置 SSH 密钥首次推送 `git@github.com:LiuXuanlings/minicyber.git`，并建立 `origin/development` 跟踪关系。
Commit：`docs: record development branch release state`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：首期 MC-001 至 MC-504 全部完成；TSAN 实际运行仍须在支持该 sanitizer 地址映射的宿主上复验。

日期：2026-08-13
任务：MC-503
状态：已完成
原生参考：本方案
改动文件：`include/minicyber/scheduler/policy/classic_context.h`、`src/scheduler/{policy/classic_context,scheduler}.cpp`、`tests/{test_pin_thread,test_smoke}.cpp`、`docs/refactor/{04_自校验定稿,00_进度记录}.md`
验证：Release 全量构建与 `ctest --test-dir build/release --output-on-failure` 为 44/44；`check_stress`、`check_cross_process` 均为 5/5，`check_scheduler_tsan`（非 TSAN 构建）为 2/2。Debug 全量构建/CTest 通过；`ASAN_OPTIONS=detect_leaks=1 ctest --test-dir build/asan --output-on-failure` 为 44/44。TSAN 预设与 `check_scheduler_tsan` 编译通过，运行在测试前失败：`ThreadSanitizer: unexpected memory mapping`。
Commit：`test: complete final validation and sanitizer cleanup`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：ASAN 修复 smoke 故意泄漏、Classic Scheduler 关闭后的静态 group 状态和容器受限 CPU 亲和性测试。TSAN 为宿主地址映射限制，须在支持 TSAN 的环境重验。MC-504 核对 `development` 分支、小粒度提交历史与远端推送。

日期：2026-08-13
任务：MC-502
状态：已完成
原生参考：对照源码链接与实施记录
改动文件：`docs/refactor/05_实施归档.md`、`docs/refactor/00_进度记录.md`
验证：实施归档中的 Markdown 链接均指向现存文件；归档声明的 CTest 目标均已注册。Release 下 `cmake --build build/release -j2 --target check_stress check_cross_process` 通过，压力与跨进程标签各 5/5 通过。
Commit：`docs: archive implementation decisions and lessons`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：归档将早期设计笔记限定为推导材料，当前行为由进度记录、取舍矩阵和性能报告共同定义；已记录 x86_64、TSAN 宿主、SHM 轮询和单次性能样本等限制。MC-503 执行最终验收并更新自校验定稿。

日期：2026-08-13
任务：MC-501
状态：已完成
原生参考：无
改动文件：`README.md`、`docs/refactor/00_进度记录.md`
验证：README 本地 Markdown 链接均指向现存文件；检索不再存在自动 SHM 路由、FastRTPS/Protobuf 已移除、Classic work-stealing 或旧性能倍率的宣传；`cmake --build build/release -j2 --target benchmark_pingpong mainboard` 通过。
Commit：`docs: align README with verified capabilities`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：README 明确自动 Transport 固定 INTRA、SHM 必须显式创建，并链接架构取舍、进度台账和一次本机性能报告。MC-502 归档架构、迭代、Bug 与学习说明。

日期：2026-08-13
任务：MC-407
状态：已完成
原生参考：无
改动文件：`examples/benchmark_pingpong.cpp`、`scripts/collect_benchmark.sh`、`docs/refactor/perf/{raw/latency_20260813T152452Z.csv,raw/latency_20260813T152452Z.json,performance_report.md}`、`docs/refactor/00_进度记录.md`
验证：`cmake --preset release -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/tmp/minicyber-googletest2` 与 `cmake --build build/release -j2 --target benchmark_pingpong benchmark_cpp20_coroutine` 通过；C++20 探针输出 `cpp20_coroutine_probe,compiled_and_resumed`；正式采集以 `WARMUP=2000 SAMPLES=10000 scripts/collect_benchmark.sh build/release/benchmark_pingpong docs/refactor/perf/raw` 生成 13 行 CSV（表头 + 4 后端 x 3 载荷），每条样本数为 10000。正式 JSON 记录 schema、父提交、`source_dirty`、CPU/内核/编译器、载荷、预热和样本。对基准运行前后的 `/dev/shm/minicyber_*` 列表比较，无新增残留。
Commit：`perf: publish reproducible performance report`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：报告仅呈现一次受控不足的本机测量，明确不作跨机制绝对排序；C++20 coroutine 因缺少等价执行器/传输语义而不纳入 CSV。MC-501 审查 README 的能力描述，仅保留已有测试或性能数据支撑的内容。

日期：2026-08-13
任务：MC-406
状态：已完成
原生参考：无
改动文件：`CMakeLists.txt`、`examples/benchmark_cpp20_coroutine.cpp`、`docs/refactor/perf/cpp20_coroutine_experiment.md`、`docs/refactor/00_进度记录.md`
验证：以 `cmake -S . -B build-cleanup -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/tmp/minicyber-googletest2` 重配后，`cmake --build build-cleanup -j2 --target benchmark_cpp20_coroutine benchmark_pingpong` 通过；`build-cleanup/benchmark_cpp20_coroutine` 输出 `cpp20_coroutine_probe,compiled_and_resumed`；`flags.make` 显示主基准仍为 `-std=c++17`，探针单独为 `-std=c++20`。
Commit：`perf: document cpp20 coroutine experiment`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：标准库 coroutine 没有执行器、跨线程通知、背压或跨进程传输；为其添加执行器将复测另一套队列/唤醒实现，不能与既有端到端 SPSC 基线稳定等价，故只保留编译/恢复探针、不写入共享性能 CSV。MC-407 生成正式性能报告及环境清单。

日期：2026-08-13
任务：MC-405
状态：已完成
原生参考：无
改动文件：`examples/benchmark_pingpong.cpp`、`scripts/collect_benchmark.sh`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target benchmark_pingpong` 通过；以 64 B、1 KiB、64 KiB 和 `--warmup 20 --samples 200` 运行，每档输出 INTRA、PIPE、SHM、`mutex_cv_queue` 四条记录，均为 200 样本。`WARMUP=10 SAMPLES=50 scripts/collect_benchmark.sh build-cleanup/benchmark_pingpong <临时目录>` 生成 13 行 CSV（表头 + 12 条记录），且三个 `mutex_cv_queue` 条目和 JSON 后端清单均存在。
Commit：`perf: add mutex condition queue baseline`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：队列容量固定为 1，消息帧完整拷贝；生产端等待消费者确认，吞吐窗口覆盖最后一条消费完成。MC-406 评估 C++20 coroutine 可选实验能否在相同口径下稳定比较。

日期：2026-08-13
任务：MC-404
状态：已完成
原生参考：SHM integration tests
改动文件：`examples/benchmark_pingpong.cpp`、`scripts/collect_benchmark.sh`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target benchmark_pingpong` 通过；`WARMUP=10 SAMPLES=50 scripts/collect_benchmark.sh build-cleanup/benchmark_pingpong <临时目录>` 生成 `minicyber_latency_v1` JSON 与单表头 CSV。CSV 为 10 行（表头 + 64 B/1 KiB/64 KiB 各 INTRA、PIPE、SHM 三条记录），每条 `samples=50`。
Commit：`perf: add cross-process shm benchmark collection`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：SHM 的发送和接收在独立子进程中初始化；控制管道保证收端就绪并限制一个在途消息，段容量按 `Sample + payload` 显式创建。MC-405 添加互斥锁与条件变量 SPSC 队列基线。

日期：2026-08-13
任务：MC-403
状态：已完成
原生参考：无
改动文件：`examples/benchmark_pingpong.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target benchmark_pingpong` 通过；分别执行 `build-cleanup/benchmark_pingpong --payload-bytes {64,1024,65536} --warmup 20 --samples 200`，每档均输出 INTRA、PIPE 两条 CSV 记录，二者 `samples` 均为 200。输出统一为 `backend,model,payload_bytes,warmup,samples,min_ns,p50_ns,p95_ns,p99_ns,max_ns,total_ns,throughput_msg_s`；吞吐量按独立测量窗口计算。
Commit：`perf: align intra and pipe latency benchmark`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：INTRA 传递共享消息对象，PIPE 写入完整帧；二者采用相同消息帧大小及 SPSC 单向端到端延迟口径，跨进程 SHM 基准与原始采集格式由 MC-404 补齐。

日期：2026-08-13
任务：MC-402
状态：已完成
原生参考：原版测试分类作参考
改动文件：`tests/CMakeLists.txt`、`docs/refactor/00_进度记录.md`
验证：重配 `build-cleanup` 后 `ctest --test-dir build-cleanup --print-labels` 显示 `unit`、`stress`、`cross_process`、`stability`、`sanitizer`、`scheduler`；`cmake --build build-cleanup -j2 --target check_stress` 为 5/5 通过；`check_cross_process` 与 `ctest -L cross_process` 为 5/5 通过；`check_scheduler_tsan` 与 `ctest -L sanitizer` 为 2/2 通过。
Commit：`test: classify CTest execution paths`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：`sanitizer` 标签标识适合在 ASAN/TSAN 预设中运行的 Scheduler 稳定性用例；实际 TSAN 运行仍受 MC-108 记录的宿主地址映射限制。MC-403 重写 INTRA/PIPE 基准并统一输出字段。

日期：2026-08-13
任务：MC-401
状态：已完成
原生参考：工程实践，不引入原版外部功能
改动文件：`CMakeLists.txt`、`CMakePresets.json`、`docs/refactor/00_进度记录.md`
验证：`cmake --preset {debug,release,asan,tsan} -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/tmp/minicyber-googletest2` 四种配置均生成成功；`cmake --build build/{debug,release} -j2 --target test_node` 通过；两处 `ctest --test-dir build/{debug,release} --output-on-failure -R '^test_node$'` 均 1/1 通过。Release `compile_commands.json` 为 `-O3 -DNDEBUG` 且无 `-O0`；ASAN/TSAN 分别含对应 sanitizer 标志。
Commit：`build: add configuration presets`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：ASAN/TSAN 本任务仅验证配置生成；运行 sanitizer 由 MC-402/后续稳定性入口执行，当前宿主的 TSAN 地址映射限制已在 MC-108 记录。MC-402 整理 CTest 标签与重复、压力、跨进程和稳定性入口。

日期：2026-08-13
任务：MC-307
状态：已完成
原生参考：`cyber/mainboard/{module_controller,module_argument,mainboard}.*`
改动文件：`include/minicyber/mainboard/module_controller.h`、`src/mainboard/module_controller.cpp`、`bin/mainboard.cpp`、`tests/test_module_controller.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_module_controller mainboard test_component_factory` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(module_controller|component_factory)$' --repeat until-fail:10`，2/2 连续 10 轮通过。
Commit：`refactor(mainboard): rollback failed DAG loads`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：LoadModule 和 LoadAll 记录进入时的组件/库水位，任何后续失败仅逆序关闭并卸载该尝试新增的资源；mainboard 在加载失败时同步关闭 Scheduler。MC-401 分离 Debug/Release/ASAN/TSAN CMake 配置。

日期：2026-08-13
任务：MC-306
状态：已完成
原生参考：`cyber/proto/{dag_conf,component_conf}.proto`
改动文件：`include/minicyber/component/component.h`、`src/mainboard/module_controller.cpp`、`tests/test_module_controller.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target minicyber_proto test_module_controller test_component test_node` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(module_controller|component|node)$' --repeat until-fail:10`，3/3 连续 10 轮通过。
Commit：`refactor(mainboard): validate DAG proto inputs`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MiniCyber 的 `dag_conf.proto` 与 `component_conf.proto` 字段编号和必要结构与原生参考一致；ReaderOption 的 `pending_queue_size` 已传至 Node/Reader，控制器拒绝缺少类名、配置、节点名、首个订阅通道或定时周期的条目。MC-307 对齐 ModuleController/mainboard 加载、失败回滚与退出。

日期：2026-08-13
任务：MC-305
状态：已完成
原生参考：`cyber/component` 与 class loader 调用点
改动文件：`include/minicyber/component/component_factory.h`、`tests/test_component_factory.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_component_factory test_module_controller test_component` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(component_factory|module_controller|component)$' --repeat until-fail:10`，3/3 连续 10 轮通过。
Commit：`refactor(component): harden factory error paths`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：ComponentFactory 拒绝空类名/空 creator；复制 creator 后在锁外构造组件，允许 creator 查询工厂而不会重入死锁。MC-306 对齐 DAG proto 必要字段及兼容性校验。

日期：2026-08-13
任务：MC-304
状态：已完成
原生参考：`cyber/component/{component_base,component,timer_component}.*`
改动文件：`include/minicyber/component/{component_base.h,component.h,timer_component.h}`、`tests/{test_component.cpp,test_timer_component.cpp}`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_component_base test_component test_timer_component test_node` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(component_base|component|timer_component|node)$' --repeat until-fail:10`，4/4 连续 10 轮通过。
Commit：`refactor(component): harden lifecycle cleanup`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：具体 Component 析构执行标准 Shutdown；初始化失败会关闭已创建的 Node/Reader，Timer 对非法 interval 或 Init 失败恢复为可重试状态。MC-305 对齐 ComponentFactory 注册、查询和错误路径。

日期：2026-08-13
任务：MC-303
状态：已完成
原生参考：`cyber/node/{reader,writer,reader_base,writer_base}.*`
改动文件：`include/minicyber/node/{node.h,node_channel_impl.h,reader.h,writer.h}`、`tests/test_node.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_node test_component test_service_client` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(node|component|service_client)$' --repeat until-fail:10`，3/3 连续 10 轮通过。
Commit：`refactor(node): add reader history semantics`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：首期保留直接回调及默认深度为 1 的有界 Reader 历史，支持 `Observe`、`ClearData` 与最新/最旧已观察消息查询；不引入原生 Blocker、协程任务、RoleAttributes 或动态拓扑。MC-304 对齐 ComponentBase/Component/TimerComponent 生命周期。

日期：2026-08-13
任务：MC-302
状态：已完成
原生参考：`cyber/node/{node,node_channel_impl}.*`
改动文件：`include/minicyber/node/{node.h,node_channel_impl.h}`、`tests/test_node.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_node test_component test_service_client` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(node|component|service_client)$' --repeat until-fail:10`，3/3 连续 10 轮通过。
Commit：`refactor(node): align endpoint ownership`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：Node 保持单 channel 单 Reader 约束；精简 NodeChannelImpl 负责空 channel 拒绝与端点初始化，Node 负责异构端点持有及显式关闭。MC-303 对齐 Reader/Writer 回调、历史与首期 QoS 子集。

日期：2026-08-13
任务：MC-301
状态：已完成
原生参考：`cyber/transport/transport.{h,cc}`
改动文件：`include/minicyber/transport/transport.h`、`tests/test_transport_routing.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_transport_routing test_topology` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(transport_routing|topology)$' --repeat until-fail:10`，2/2 连续 10 轮通过；`rg -n -i 'rtps' --glob '!cyber_ref/**' --glob '!docs/**' include src tests CMakeLists.txt` 未发现 RTPS 路由实现或入口。
Commit：`refactor(transport): stabilize local route selection`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MiniCyber 不具备原生 HybridTransport 所需的动态拓扑发现和端点变更通知，自动工厂固定选择 INTRA，避免端点创建顺序或静态拓扑后续变化造成 INTRA/SHM 分流；跨进程调用方继续显式使用 SHM 收发器。MC-302 对齐 Node 与 NodeChannelImpl 生命周期和持有关系。

日期：2026-08-13
任务：MC-001
状态：已完成
原生参考：无
改动文件：`docs/refactor/baseline.md`、`docs/refactor/00_进度记录.md`
验证：以 Debug 配置 `build-baseline` 成功；`cmake --build build-baseline -j2` 失败（GNU as 的 `--defsym minicyber_core_EXPORTS` 无值）；`ctest --test-dir build-baseline --output-on-failure` 注册 47 个测试，0 通过、47 Not Run（测试可执行文件未生成）。
Commit：`docs(refactor): record MC-001 baseline`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：GoogleTest 镜像 `kkgithub.com` 不可用，使用 GitHub 的 `release-1.12.1` 临时源码覆盖完成配置；构建失败留待覆盖构建/汇编问题的后续任务处理。

日期：2026-08-13
任务：MC-002
状态：已完成
原生参考：`cyber/{croutine,scheduler,data,transport,node,component,mainboard}`
改动文件：`docs/refactor/module_mapping.md`、`docs/refactor/00_进度记录.md`
验证：对照首期六个 `cyber_ref` 子树与 MiniCyber 路径；`test -f` 路径检查通过；`rg` 完成旧 Fiber/Context/IOManager/Thread/根级 Scheduler 的引用检查。
Commit：`docs(refactor): map MC-002 modules`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MC-003 仅删除根级 Fiber 架构及引用；`src/swap.S` 同时是旧 Context 和后续 CRoutine 上下文边界，不能在 MC-003 中删除。

日期：2026-08-13
任务：MC-003
状态：阻塞
原生参考：无（非原生架构）
改动文件：`docs/refactor/00_进度记录.md`
验证：`rg -n -i '#include [<"]minicyber/fiber\\.h[>"]|\\bFiber\\b' --glob '*.{h,hpp,cpp,S}' --glob 'CMakeLists.txt' include src tests examples bin CMakeLists.txt` 显示 `include/minicyber/scheduler.h`、`src/scheduler.cpp`、`include/minicyber/iomanager.h`、`src/iomanager.cpp` 和 `src/context.cpp` 仍依赖 Fiber；根 CMake 以 `file(GLOB_RECURSE SRC_FILES "src/*.cpp" "src/*.S")` 收集这些源文件，tests CMake 以 `test_*.cpp` 收集测试。
Commit：`docs(refactor): record MC-003 blocker`（仅记录阻塞，未做产品代码删除；以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：任务卡要求 MC-003 独立删除 Fiber 并通过全量编译，但其全部实现引用被任务卡分配给后续 MC-004 至 MC-006；删除 Fiber 后旧 Scheduler/IOManager/Context 无法编译。需要确认：允许将 MC-003 至 MC-006 合并为一次旧架构清理，或要求先在 MC-003 将这些消费者迁移到新 CRoutine/Scheduler（这会跨越后续任务边界）。

日期：2026-08-13
任务：MC-003 至 MC-006
状态：已完成（用户授权合并执行）
原生参考：`cyber/croutine/detail/routine_context.*`；其余为非原生旧架构清理。
改动文件：删除根级 `fiber`、`context`、`iomanager`、`thread`、旧 `scheduler` 实现及专属测试/示例；新增 `include/minicyber/croutine/detail/routine_context.h`、`src/croutine/detail/routine_context.cpp`；更新 CRoutine、CMake 和 tests CMake。
验证：以 `build-cleanup` Debug 配置执行 `cmake --build build-cleanup -j2`，全量构建通过；旧根级头文件引用与 `test_fiber`、`test_epoll`、`test_thread`、`test_scheduler` 的 CTest 注册均为零；`ctest --test-dir build-cleanup --output-on-failure` 为 42/43 通过，唯一失败为既有 `test_shm_segment.ReopenAfterClose` 对 malloc 地址不应复用的断言。
Commit：`refactor: remove legacy runtime stack`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：阶段 A 的旧架构清理已完成；SHM 段重开地址断言失败与本次清理无关，应由相关 SHM 任务单独处理。

日期：2026-08-13
任务：MC-101
状态：已完成
原生参考：`cyber/croutine/croutine.{h,cc}`
改动文件：`include/minicyber/croutine/croutine.h`、`src/croutine.cpp`、`tests/test_croutine.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_croutine test_processor test_classic_context test_choreography_context test_processor_context`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(croutine|processor|classic_context|choreography_context)'`，5/5 通过。
Commit：`refactor(croutine): align MC-101 lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MC-102 对齐上下文栈布局、入口参数和汇编切换边界；本任务未改动这些实现细节。

日期：2026-08-13
任务：MC-102
状态：已完成
原生参考：`cyber/croutine/detail/{routine_context.*,swap_x86_64.S}`
改动文件：`include/minicyber/croutine/detail/routine_context.h`、`src/croutine/detail/routine_context.cpp`、`src/swap.S`、`tests/test_croutine.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_croutine test_processor test_processor_context`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(croutine|processor|processor_context)'`，3/3 通过；最终单独执行 `test_croutine` 通过。
Commit：`refactor(croutine): align MC-102 context switch`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：当前实现与验证覆盖 x86_64；尚未提供 aarch64 汇编实现，跨架构支持须在平台引入时单独处理。

日期：2026-08-13
任务：MC-103
状态：已完成
原生参考：`cyber/scheduler/{processor,processor_context}.{h,cc}`
改动文件：`include/minicyber/scheduler/processor.h`、`src/scheduler/processor.cpp`、`tests/test_processor.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_processor test_processor_context test_classic_context test_choreography_context`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(processor|processor_context|classic_context|choreography_context)'`，4/4 通过。
Commit：`refactor(scheduler): align MC-103 processor loop`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MC-104 对齐 ClassicContext 的队列、优先级和 work-stealing 语义；Processor 仅负责消费 Context 返回的已获取协程。

日期：2026-08-13
任务：MC-104
状态：已完成
原生参考：`cyber/scheduler/policy/classic_context.{h,cc}`
改动文件：`include/minicyber/scheduler/policy/classic_context.h`、`src/scheduler/policy/classic_context.cpp`、`tests/test_classic_context.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_classic_context test_processor`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(classic_context|processor)$'`，2/2 通过。
Commit：`refactor(scheduler): align MC-104 classic context`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MC-105 对齐 Classic Scheduler 的启动、唤醒、停止与绑核协作；ClassicContext 只实现策略上下文，不负责线程生命周期。

日期：2026-08-13
任务：MC-105
状态：已完成
原生参考：`cyber/scheduler/policy/scheduler_classic.*`、`scheduler/common/pin_thread.*`
改动文件：`include/minicyber/scheduler/{scheduler.h,scheduler_conf.h,common/pin_thread.h}`、`src/scheduler/scheduler.cpp`、`tests/test_scheduler_classic.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_scheduler_classic test_pin_thread`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(scheduler_classic|pin_thread)$'`，2/2 通过。
Commit：`refactor(scheduler): align MC-105 classic lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MC-106 对齐 ChoreographyContext 的协程绑定、目标处理器与释放语义；不修改 Classic Scheduler 生命周期。

日期：2026-08-13
任务：MC-106
状态：已完成
原生参考：`cyber/scheduler/policy/choreography_context.{h,cc}`
改动文件：`include/minicyber/scheduler/policy/choreography_context.h`、`src/scheduler/policy/choreography_context.cpp`、`tests/test_choreography_context.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_choreography_context test_processor`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(choreography_context|processor)$'`，2/2 通过。
Commit：`refactor(scheduler): align MC-106 choreography context`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：原生 `processor_id` 配置和定向路由位于 `SchedulerChoreography::DispatchTask/NotifyProcessor`，留给 MC-107 的策略工厂、启动和停止实现。

日期：2026-08-13
任务：MC-107
状态：已完成
原生参考：`cyber/scheduler/{scheduler_factory.*,policy/scheduler_choreography.*}`
改动文件：`include/minicyber/scheduler/{scheduler.h,scheduler_conf.h,scheduler_factory.h}`、`src/scheduler/{scheduler.cpp,scheduler_factory.cpp}`、`tests/test_scheduler_choreography.cpp`、`docs/refactor/00_进度记录.md`
验证：重新配置 `build-cleanup` 后执行 `cmake --build build-cleanup -j2 --target test_scheduler_choreography test_scheduler_classic`；`ctest --test-dir build-cleanup --output-on-failure -R 'test_scheduler_(choreography|classic)$'`，2/2 通过。
Commit：`refactor(scheduler): add MC-107 choreography routing`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：MC-108 增加关闭、并发提交和策略切换稳定性测试及 TSAN 入口；当前 Choreography 测试已覆盖定向执行和唤醒的基本链路。

日期：2026-08-13
任务：MC-108
状态：已完成
原生参考：`cyber/scheduler/{scheduler.*,scheduler_test.cc,scheduler_classic_test.cc,scheduler_choreo_test.cc}`
改动文件：`include/minicyber/scheduler/scheduler.h`、`src/scheduler/scheduler.cpp`、`tests/{CMakeLists.txt,test_scheduler_classic.cpp,test_scheduler_choreography.cpp}`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target check_scheduler_tsan` 通过；`ctest --test-dir build-cleanup --output-on-failure -L tsan --repeat until-fail:10`，2/2 连续 10 轮通过。`cmake -S . -B build-tsan -DUSE_SANITIZER=TSAN ...` 与 `cmake --build build-tsan -j2 --target check_scheduler_tsan` 完成编译，但运行时在进入测试前失败：`ThreadSanitizer: unexpected memory mapping`。
Commit：`refactor(scheduler): stabilize shutdown submission`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：TSAN 失败由当前运行环境地址映射限制触发，非项目诊断；可在支持 TSAN 的宿主机以 `cmake -S . -B build-tsan -DUSE_SANITIZER=TSAN`、`cmake --build build-tsan --target check_scheduler_tsan` 复验。MC-201 开始阶段 C 的 CacheBuffer/ChannelBuffer 语义对齐。

日期：2026-08-13
任务：MC-201
状态：已完成
原生参考：`cyber/data/{cache_buffer.h,channel_buffer.h,cache_buffer_test.cc,channel_buffer_test.cc}`
改动文件：`tests/{test_cache_buffer.cpp,test_channel_buffer.cpp}`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_cache_buffer test_channel_buffer test_data_dispatcher test_data_visitor` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(cache_buffer|channel_buffer|data_dispatcher|data_visitor)$' --repeat until-fail:10`，4/4 连续 10 轮通过。
Commit：`test(data): verify MC-201 buffer semantics`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：CacheBuffer 与 ChannelBuffer 实现原有的有效容量 `size`（内部 `size + 1`）、满时覆盖最旧项、绝对索引窗口、游标落后快进至最新项和 oldest-first 批量读取均与原生参考一致；本任务补齐最小容量覆盖及零批量读取回归。MC-202 对齐 DataDispatcher 注册、分发、注销和并发边界。

日期：2026-08-13
任务：MC-202
状态：已完成
原生参考：`cyber/data/{data_dispatcher.h,data_dispatcher_test.cc}`
改动文件：`include/minicyber/data/data_dispatcher.h`、`tests/test_data_dispatcher.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_data_dispatcher test_data_visitor test_intra_transmitter test_intra_receiver` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(data_dispatcher|data_visitor|intra_transmitter|intra_receiver)$' --repeat until-fail:10`，4/4 连续 10 轮通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_data_dispatcher$' --repeat until-fail:30`，1/1 连续 30 轮通过。
Commit：`refactor(data): synchronize dispatcher lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：保留原版“分发后通知”的行为，并扩展显式 `RemoveBuffer`。频道表采用短锁保护和弱引用快照，分发/通知在锁外执行，避免注册替换与并发分发的悬垂指针，同时允许通知回调重入分发。MC-203 对齐 DataNotifier/Visitor 的数据等待和唤醒链路。

日期：2026-08-13
任务：MC-203
状态：已完成
原生参考：`cyber/data/{data_notifier.h,data_visitor_base.h,data_visitor.h}`
改动文件：`include/minicyber/data/{data_notifier.h,data_visitor_base.h,data_visitor.h,fusion/all_latest.h}`、`include/minicyber/transport/receiver/{intra_receiver.h,shm_receiver.h}`、相关 data/transport 测试、`docs/refactor/00_进度记录.md`
验证：构建 `test_data_notifier test_data_visitor test_data_fusion test_fusion_all_latest test_data_dispatcher test_intra_receiver test_shm_transmitter` 通过；上述前六项 `ctest --repeat until-fail:10`，6/6 连续 10 轮通过；`test_shm_transmitter` 连续 3 轮通过。
Commit：`refactor(data): synchronize notifier lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：Notify 在短锁内获取 shared_ptr 快照、锁外执行；RemoveNotifier 先摘除路由后等待跨线程进行中的回调结束，且支持回调内自注销而不自锁。DataVisitor、AllLatest、INTRA/SHM receiver 均在销毁或 Disable 时解除 notifier/buffer 注册。MC-204 对齐 AllLatest/DataFusion 多输入触发规则。

日期：2026-08-13
任务：MC-204
状态：已完成
原生参考：`cyber/data/fusion/{data_fusion.h,all_latest.h,all_latest_test.cc}`
改动文件：`include/minicyber/data/{cache_buffer.h,data_visitor.h,fusion/data_fusion.h,fusion/all_latest.h}`、`tests/{test_cache_buffer.cpp,test_data_fusion.cpp,test_fusion_all_latest.cpp}`、`docs/refactor/00_进度记录.md`
验证：构建 `test_cache_buffer test_channel_buffer test_data_dispatcher test_data_notifier test_data_visitor test_data_fusion test_fusion_all_latest` 通过；对应 7 项 CTest 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(cache_buffer|data_fusion|fusion_all_latest|data_visitor|data_dispatcher)$' --repeat until-fail:10`，5/5 连续 10 轮通过。全量 `cmake --build build-cleanup -j2` 在未触及的 `examples/benchmark_pingpong.cpp` 失败：`minicyber::Duration` 在 `croutine.h` 的别名与 `time/time.h` 的类重名，及随后的 `ToNanosecond` 缺失。
Commit：`refactor(data): align all-latest fusion`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：AllLatest 仅由 primary Fill 触发，所有 secondary 必须已有最新值；融合项保留该次 primary 消息及各 secondary 最新消息。CacheBuffer 在替换/清除融合回调时等待 in-flight 回调结束。DataVisitor 在成功读取后推进游标，与原生 DataFusion 的非推进 `Fusion()` 契约一致。全量构建失败属于 Time/CRoutine Duration 命名冲突，留给对应时间/构建任务处理。MC-205 对齐 INTRA dispatcher、收发器和 listener 生命周期。

日期：2026-08-13
任务：MC-205
状态：已完成
原生参考：`cyber/transport/dispatcher/intra_dispatcher.h`、`transport/{transmitter/intra_transmitter.h,receiver/intra_receiver.h}`
改动文件：`include/minicyber/transport/{receiver/intra_receiver.h,transmitter/intra_transmitter.h}`、`tests/{test_intra_receiver.cpp,test_intra_transmitter.cpp}`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_intra_dispatcher test_intra_transmitter test_intra_receiver test_transport_routing` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_intra_(dispatcher|transmitter|receiver)$' --repeat until-fail:20`，3/3 连续 20 轮通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_transport_routing$'`，1/1 通过。
Commit：`refactor(transport): align intra lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：INTRA receiver 将 buffer/notifier 封装为订阅会话；Disable 先摘除通知、等待其他线程的在途回调后释放 buffer，支持回调内自注销和并发重复 Enable。INTRA transmitter 序列化启停及同步发布，防止 Disable 与发布交叉。扩展 SHM receiver 回归时发现其 `MultipleMessages`、`ForkCrossProcessReceive`、`ChannelIsolation` 在未改 SHM 实现的情况下失败，留待 MC-206 至 MC-209 专项处理。

日期：2026-08-13
任务：MC-206
状态：已完成
原生参考：`cyber/transport/shm/{block,state,segment}.{h,cc}`
改动文件：`include/minicyber/transport/shm/{segment.h,posix_segment.h}`、`src/transport/shm/posix_segment.cpp`、`tests/{test_shm_segment.cpp,test_posix_segment.cpp}`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_shm_block test_shm_state test_shm_segment test_posix_segment test_posix_segment_signal test_shm_transmitter test_shm_receiver` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(shm_block|shm_state|shm_segment|posix_segment|posix_segment_signal)$' --repeat until-fail:20`，5/5 连续 20 轮通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_shm_(transmitter|receiver)$'`，2/2 通过。
Commit：`refactor(shm): align segment block ownership`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：将 Block 读写视图和对应 release 提升到 Segment 契约，由 PosixSegment 实现。写入分配在首个 ring block 忙碌时继续探测其他块，所有块均忙才失败；release 校验视图确属当前段，避免外来指针错误解锁。修正了 MockSegment 重开测试中不保证的地址不复用假设。MC-207 处理 POSIX 段创建、映射、析构和异常清理。

日期：2026-08-13
任务：MC-207
状态：已完成
原生参考：`cyber/transport/shm/posix_segment.{h,cc}`
改动文件：`include/minicyber/transport/shm/posix_segment.h`、`src/transport/shm/posix_segment.cpp`、`tests/test_posix_segment.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_posix_segment test_posix_segment_signal test_shm_transmitter test_shm_receiver` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(posix_segment|posix_segment_signal)$' --repeat until-fail:10`，2/2 连续 10 轮通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(shm_block|shm_state|shm_segment|posix_segment|posix_segment_signal|shm_transmitter|shm_receiver)$'`，7/7 通过。
Commit：`refactor(shm): harden posix segment lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：Open 拒绝零容量/零块配置和算术溢出；OpenOnly 在采用已有映射前校验完整 State/Block/payload 布局，失败后解除映射并复位状态。Destroy 仅在最后一个共享引用离开时 unlink，避免一个 peer 销毁仍在使用的通道。Close 仍只解除当前映射，符合现有公开接口。MC-208 对齐 ConditionNotifier 通知、超时和退出语义。

日期：2026-08-13
任务：MC-208
状态：已完成
原生参考：`cyber/transport/shm/{condition_notifier,readable_info}.{h,cc}`
改动文件：`include/minicyber/transport/shm/condition_notifier.h`、`src/transport/shm/condition_notifier.cpp`、`tests/test_condition_notifier.cpp`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_condition_notifier test_shm_dispatcher test_shm_transmitter test_shm_receiver` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_condition_notifier$' --repeat until-fail:20`，1/1 连续 20 轮通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(condition_notifier|shm_dispatcher|shm_transmitter|shm_receiver)$'`，4/4 通过。
Commit：`refactor(shm): synchronize condition notifier`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：共享环中的槽位先写入 payload，再以 release 发布槽序列和全局提交序号；消费者以 acquire 读取，避免读取半写通知。多发布者经共享 writer lock 串行提交。`Listen(-1)` 现在等待至通知或 Shutdown；Shutdown 先阻止新操作、等待在途 Listen/Notify 离开映射后再 detach。MC-209 对齐 SHM dispatcher、收发器和跨进程注册/回收。

日期：2026-08-13
任务：MC-209
状态：已完成
原生参考：`cyber/transport/{dispatcher/shm_dispatcher.*,transmitter/shm_transmitter.h,receiver/shm_receiver.h}`
改动文件：`include/minicyber/transport/{dispatcher/shm_dispatcher.h,transmitter/shm_transmitter.h,receiver/shm_receiver.h}`、`src/transport/dispatcher/shm_dispatcher.cpp`、`tests/{test_shm_dispatcher.cpp,test_shm_transmitter.cpp,test_shm_receiver.cpp}`、`docs/refactor/00_进度记录.md`
验证：`cmake --build build-cleanup -j2 --target test_shm_dispatcher test_shm_transmitter test_shm_receiver test_transport_routing` 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(shm_dispatcher|shm_transmitter|shm_receiver|transport_routing)$'`，4/4 通过；`ctest --test-dir build-cleanup --output-on-failure -R 'test_(shm_dispatcher|shm_transmitter|shm_receiver)$' --repeat until-fail:10`，3/3 连续 10 轮通过。
Commit：`refactor(transport): synchronize shm lifecycle`（以 `git log -1 --oneline` 获取提交哈希）
阻塞或后续：dispatcher 以互斥锁保护 segment 注册表，并在锁外以 shared_ptr 快照读取，Shutdown 先停止/等待线程再清表。SHM receiver 使用可安全注销的订阅会话，支持 Disable/Enable 而不保留旧 callback；transmitter 串行化启停/发布、拒绝空消息且仅在通知成功后递增序号。POSIX 段由 dispatcher 持有时 transmitter Disable 只释放自己的引用，物理回收由最后持有者决定。MC-301 开始阶段 D 的 Transport 本地 INTRA/SHM 选择对齐。
